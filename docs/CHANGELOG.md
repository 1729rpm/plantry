# CHANGELOG

Append-only chronology of shipped changes. One entry per change. Newest first.

Format:

```
## YYYY-MM-DD  short title

Brief description in present tense, one to three sentences. Reference the PR.
```

---

## 2026-06-21 Grocery day selection persists across page and app switches

The Grocery screen's "Order for" day chips now persist to `localStorage` instead of living only in in-memory React state, so a household member's chosen days survive switching tabs inside the app (component unmount) and backgrounding or reopening the PWA (memory eviction) rather than reverting to the time-aware default. New `getGroceryDays`/`setGroceryDays` helpers in `app/web/src/lib/storage.ts` store a single `{ weekStart, days }` record under `plantry:groceryDays`; a read for a different `weekStart` is ignored, so a fresh week starts clean on the time-aware default. The selection is seeded once per week through a ref-guarded effect (it never re-seeds and stomps a fresh toggle), and a stored day that has since become past or skipped is dropped on seed. A non-empty stored selection that fully expires shows the pick-a-day prompt rather than re-defaulting. (#176)

## 2026-06-21 Within-week cuisine diversity (§4 step 5): menu leans slightly less Indian

A soft, target-gated §4 selection step nudges the generated week toward a small, fixed number of non-Indian dishes (`cuisine !== "Indian"`) on a standing basis without flipping the menu international. While fewer than `WEEKLY_NON_INDIAN_TARGET` (3) non-Indian dishes have been placed in the week being generated, non-Indian candidates rank above Indian ones in each slot's pool; once the target is met the step is a no-op. It sits after Preferred=Yes and before the two terminal within-week partitions (recency, protein diversity), so it can promote a non-Indian dish over a Preferred Indian one but never forces a dish repeat or HP-protein clash. Soft with a fresh-alternative fallback (an all-Indian lunch-carb or fruit pool is a no-op), so it never narrows §3 eligibility. This turns `cuisine` into a §4 rule input (§1 eligibility and §3 composition still never read it); `docs/engine.md` §4 and §12 updated. The five simulated weeks move from 0 to 4-5 non-Indian dishes each. Closes the Cuisine diversity feature (single stream A). (#175)

## 2026-06-19 Crawl-harness readiness wait + Grocery gutter selectors

The `app/web/e2e/smoke.mjs` crawl now waits on a per-tab post-hydration selector (Menu `.day-card`, Grocery `.grocery-list`, Explore `.explore-card`, Changes `.screen__list`) after each tab click instead of a fixed delay after `networkidle`, so the asserts no longer fire before live Convex data hydrates over the websocket and a tab stops reporting a false "not found." `SCREEN_GUTTER_CANDIDATES` adds the current `.grocery-chooser` and `.grocery-list`, which it had been missing since the Grocery rewrite. RETRO intake. (#174)

## 2026-06-19 Seeded dev Convex test week for UI verification

`scripts/seed-dev-week.mjs` seeds the dev Convex deployment (`lovely-curlew-631`, never prod) with a real generated current week by running `generateWeek:generateCurrentWeek`, so the UI crawl can render the read-path screens (Grocery, the Menu week) and exercise new write mutations against a live-shaped week instead of an empty deployment. It reuses the real generation path so it cannot drift from the schema, and is idempotent. RETRO intake. (#174)

## 2026-06-19 Crawl exercises affordance behaviour; §16 documents the seeded week

`docs/engineering.md` §16 and `docs/development.md` §3 now state the verification crawl clicks every new interactive affordance and asserts the resulting state, not only screenshots it. §16's residual bullet documents that the dev Convex carries a seeded test week the crawl renders read-path screens (Grocery) and exercises new write mutations against, with the static-`index.css`-render of a hand-built grocery DOM as the fallback for verifying Grocery-list CSS when a seeded week is unavailable. RETRO intake. (#174)

## 2026-06-19 Engineer brief: bake a fresh worktree before typecheck/build/test

`.claude/commands/new-stream.md` gains a default brief line instructing engineers to run `npm install && npm run bake` before any typecheck/build/test, because `engine/src/data/library.ts` and `history.ts` are generated-and-gitignored and a fresh worktree otherwise hits a confusing "missing module" failure. RETRO intake. (#174)

## 2026-06-18 Menu title aligns flush to the top of its header

The Menu wordmark now sits flush at the top of its header, level with the Explore, Grocery, and Changes titles, instead of a few pixels lower. The avatar button keeps its 44px tap target and reads centered on the wordmark. Frontend only. (#168, #169)

## 2026-06-18 Close the tab title-to-subtitle gap to zero

The space between the title and the subtitle on all four tab screens (Menu, Explore, Grocery, Changes) closes from a loose gap to flush, so the subtitle sits directly beneath the title. Frontend only. (#166)

## 2026-06-18 Grocery card item rows align with their group label

The item rows inside each grocery category card now left-align with the group label, and the dead space below the last row of each card is removed. The list (a `<ul>`) had kept the browser's default list indent and bottom margin, so every item name sat about 40px to the right of its label and each card carried extra padding at the bottom. Frontend only. (#165)

## 2026-06-18 Drop the archived design_handoff path from the CI repo-structure allowlist

The repo-structure CI check no longer lists the old root-level `design_handoff/` path, which was archived earlier; the allowlist now matches the actual tree. Tooling only. (#164)

## 2026-06-18 Menu home: collapsed past-day cards use a chevron, not a "View" pill

Each collapsed past-day card on the Menu home replaces its "View" text pill with a bare iOS-style right-chevron disclosure indicator at the row's right edge: a subtle gray chevron with no border or fill, the way a list row signals "tap to open". It stays a real button with the same tap target and an accessible "View {day}" label, and opens the same day editor; behavior is unchanged. Frontend only. (#160)

## 2026-06-18 Unified title and subtitle styling across the four tab screens

The Menu, Explore, Grocery, and Changes screens now share one title treatment: a 30px/700 serif title with a 12px gap to a 14px subtitle, so the four bottom-nav screens read as one family instead of the Menu wordmark sitting larger than the rest. Frontend only. (#161)

## 2026-06-18 App-level error boundary degrades a screen crash gracefully

An app-level error boundary wraps the screen surface, so an unhandled error on any screen degrades to a recoverable fallback (the tab bar survives and the user can switch tabs or retry) instead of blanking the whole app. Frontend only. (#157)

## 2026-06-18 Grocery buy list is now day-selected (frontend)

The Grocery screen gains a day chooser: the household picks which upcoming days to order for and the list totals exactly those days. The default is time-aware off the device clock (today + tomorrow before 11 AM, tomorrow + day-after from 11 AM). Renders in the compact layout so the day chooser and the list read in one glance. Calls the new `selectedDays` arg on `getGroceryList`. Frontend only. (#155)

## 2026-06-18 Day-selected grocery list (backend selectedDays arg)

`getGroceryList` gains an optional `selectedDays` arg so the buy list totals exactly the chosen upcoming days instead of the full week. Backward-compatible (omitting it preserves the prior whole-week behavior); shipped and deployed backend-first, ahead of the frontend, so the new arg exists in prod before any client requests it. Convex only. (#156)

## 2026-06-18 Grocery groups restructured; generic Seasonal fruit deactivated

The grocery list group order becomes `Proteins and Dairy · Fruit · Vegetables · Aromatics and Herbs · Pantry`: Fruit is its own group, the "Other" catch-all is removed, and Pantry renders last and is the fallback bucket for any ungrouped ingredient. The generic "Seasonal fruit" dish (id 123) is deactivated (`active: No`), so it is retired from menus, Explore, and swaps while the library still resolves id 123 for the history seed; specific fruit dishes now cover every season. Structural canonical-data + engine + spec change. (#154)

## 2026-06-18 Menu home: past days render as compact collapsed tiles

Collapsed past-day cards on the Menu render as slimmer compact tiles (bare date number, tighter padding, smaller "View" pill), so finished days recede visually. Frontend only. (#152)

## 2026-06-18 Grocery Day Selection feature baseline / handoff

Lands the Grocery Day Selection + Compact Passes design handoff and the EM stream-state plan, sets the CLAUDE.md "Currently building" line, and records the grouping decision. Docs / housekeeping baseline for the feature. (#151)

## 2026-06-18 Run the app/web test suite in CI; fix stale Pav meal-time tests

CI now runs `npm test --workspace @plantry/web` as a `Test web` step alongside the engine suite, so the frontend vitest tests (previously never run in CI) are gated on every PR and can no longer rot silently. Fixes the two `app/web/test/library.test.ts` `addableMeals`-gating tests that hard-coded Pav (id 281) as a breakfast dish and broke on the #143 Pav-to-lunch flip, rewriting them as property-based assertions on the gating invariant (the pool is non-empty, every dish matches the requested meal, and no other-meal dish leaks through) so no single dish id can rot them again. Data left correct at lunch; CI config and test logic only. (#150)

## 2026-06-18 Changes nav badge becomes an unread counter

The Changes tab badge now counts only this week's menu edits made by the other user that the viewer has not yet seen, instead of the never-clearing total of all edits. Opening the Changes tab marks the loaded feed seen at its newest server timestamp, which clears the badge; a later edit by the other user re-raises it; the viewer's own edits never count. The seen marker is stored per identity in local storage. The Changes subtitle and feed are unchanged. Frontend only. (#147)

## 2026-06-18 Anchor header top padding to the safe-area inset

Both top headers replace a fixed `padding-top: 54px` with `calc(env(safe-area-inset-top, 0px) + 24px)`, so every device shows exactly 24px below whatever it reserves at the very top (status bar, notch, Dynamic Island, display cutout) and degrades to a clean 24px on browsers with no top inset. `viewport-fit=cover` (already set) makes `env(safe-area-inset-top)` resolve to the real reserved strip. The shared `.screen__header` rule covers all four tab screens (Menu, Explore, Changes, Grocery); `.day-screen__header` covers the day editor. Kept as a standalone longhand, never folded into a padding shorthand (the stylelint env()-in-shorthand guard). Frontend only. (#148)

## 2026-06-18 Correct meal-time labels for Pav (lunch) and boiled eggs (breakfast)

Flips two `time` fields so each dish reads and orders under the correct meal in the swap picker and Explore: `data/dishes/pav.md` Breakfast to Lunch and `data/dishes/boiled-eggs.md` Lunch to Breakfast. Data-only, with no engine rule (offered and declined), so neither dish auto-composes at its new meal: Pav leaves the breakfast Option C carb pool and the lunch-carb rule still reads only Chapati and Rice; boiled eggs leave the lunch Keto pool and breakfast has no Keto slot. Both stay reachable via swap and Explore, which is where the labels were wrong. Reverses the 2026-06-16 `Pav time:Breakfast` decision (#120/#123). Pool safety verified: 13 active Keto-lunch dishes and 15 active breakfast Option-C carbs remain, so nothing strands. (#143)

## 2026-06-17 Close out the UI Improvements feature

Archives the UI Improvements design handoff and EM plan (`features/UI Improvements/` to `archive/features/UI Improvements/`, keeping `features/.gitkeep`) and resets the CLAUDE.md "Currently building" line to none, the last step of the feature. Per `claude-design.md`, between features the design reference reverts to the live app until the next handoff is commissioned. Docs / housekeeping only. (#142)

## 2026-06-17 Add a custom dish from the Add sheet; rename one-off to custom dish (UI)

The Add-a-dish sheet gains an "Add '{name}' as a custom dish" affordance that appends a custom (free-text, not-in-library) dish as an extra dish on the day, wired to the `appendCustomDish` mutation, with a minimal breakfast/lunch selector for the custom-dish case (auto-routed when the day has a single addable meal) and the required reason. Library dishes still auto-route by their own meal-time. The user-facing "one off" wording is renamed to "custom dish" across the day card, day editor, dish rows, the swap picker, the share image, and the meta-line fallback; the internal `changeKind`/`source` value stays `custom` (Principle 7). Frontend only. (#141)

## 2026-06-17 Note for the weekly review: day-level comment entry returns (UI)

The day editor gains a "Note for the weekly review" card: an always-visible textarea and a Post comment pill (disabled until there is text), wired to the existing `addComment` mutation as a day-attached queued comment. It is record-only with no required reason; it changes nothing in the week and surfaces in the Changes feed (Principle 5). Restores the affordance the #78 UI removal had dropped. Frontend only. (#140)

## 2026-06-17 Append a custom dish as an extra dish (backend)

Adds the `appendCustomDish` Convex mutation: a custom (free-text, not-in-library) dish can be appended as an extra dish to a breakfast or lunch slot, not only replace a position. It mirrors `addDish` (append plus the new position) and `addCustomOneOff` (the custom pick payload), records a `changeKind: "custom"` manual-change row with a null `before`, and does not enforce the per-day item cap (the fast loop is permissive, and the cap is a generation-time constraint only). Reverses the `docs/product.md` §7 "appending is out of scope" stance and aligns the spec's "one-off" wording to "custom dish"; `MAINTENANCE.md` notes that a `custom` row may now be an append. No schema or engine change. Convex deploy verified on prod. (#139)

## 2026-06-17 Explicit close (x) button on the bottom sheet (UI)

Every bottom sheet gains a circular close (x) button at the top-right (surface fill, soft shadow) alongside the drag handle, so a sheet can be dismissed explicitly, not only by drag, scrim tap, or Back. It calls the same `onClose` the scrim uses, so it unwinds through the unified back-stack; no separate dismissal path. Shared `Sheet` primitive, so it appears on every overlay. Frontend only. (#137)

## 2026-06-17 Re-allow day-level comment entry (product spec)

`docs/product.md` re-introduces day-level comment entry to the day editor (§2 weekly loop, §6 scope): a free-text note about the day that changes nothing in the week and queues for the slow-loop review, record-only and with no required reason (Principle 5). It restores the spec stance that the earlier UI removal (#78) left without an affordance; the queued-comments backend (`addComment`, the `comments` table, and the Changes-tab render) already exists, so this is spec-only. The day-editor UI follows in a separate slice (Stream L). (#138)

## 2026-06-17 Archive the first design handoff and its catch-up

Moves the superseded first handoff `design_handoff/` to `archive/handoffs/design_handoff/` and the now-folded-in `design-catchup.md` to `archive/handoffs/design-catchup.md`, each with an archival banner explaining the move. The `features/UI Improvements/` handoff (level with the live app) replaces both, so keeping a stale handoff at the repo root no longer serves the per-slice design-compare gate. The eslint ignore follows the move (`design_handoff/**` becomes `archive/**`, so retired reference JSX is not linted). Docs / housekeeping only. (#136)

## 2026-06-17 Collapse past days on the Menu (UI Improvements J2)

Days before today collapse to a compact card: a date-number-only badge, a one-line glance of the day's first two dish names with a "+N more" tail (or "Skipped"), and a "View" pill that opens the same day editor as Edit. Today and upcoming days stay full; today's card carries the accentSoft border. Adds `dayStatus()` (a device-local calendar comparison, unit-tested) and rebrands the Menu loading and empty-state fallback headers to the Plantry wordmark. Frontend only. (#134)

## 2026-06-17 Brand-led Menu header, change count moves to the Changes tab (UI Improvements J1)

The Menu header leads with the Plantry serif wordmark and a full-month date subtitle ("June 15 to June 20 menu"); the in-header change-summary line is removed. The week's change count now lives on the Changes tab: its subtitle reads "{n} changes to this week's menu" (n excludes comments, falling back to the quiet zero-state line) and the Changes nav tab carries a matching count badge, hidden at zero. Adds `weekRangeLabelLong()`. Frontend only. (#133)

## 2026-06-17 Generic picker search with dynamic, result-driven filters (UI)

The Add-a-dish and Replace pickers become one generic search over the active library. Add drops its Breakfast/Lunch selector and routes a chosen dish to the slot its own meal-time names (a breakfast dish lands in breakfast, a lunch dish in lunch); Replace surfaces cross-meal dishes, so a breakfast dish like Pav is now reachable from a lunch slot. Both sheets render a single filter row below the search bar whose pills (Breakfast, Lunch, Easy to cook, Healthy) appear only when the current results contain a matching dish and reset when the search text changes; the Fruit slot keeps its quality-only pills. Frontend only; carries the picker-filter lib helpers. (#131)

## 2026-06-17 Generic (non-meal-time) swap pool plus slot-meal-first ordering (backend)

The breakfast/lunch swap pool is no longer hard-filtered by meal-time: it is every Active, in-season, non-Fruit dish, so a cross-meal pick is allowed and the resulting §3 composition mismatch becomes slow-loop signal rather than a blocked action (per `docs/product.md` §4 Principle 4). `getSlotAlternatives` stable-partitions the ranked result so slot-meal-matching dishes lead the default suggested view while the full pool stays reachable by search and filter pills. `swapDish` drops the `dish-not-meal-time` rejection and adds the inverse `dish-is-fruit` guard (a fruit cannot be swapped into a meal slot). Canonical docs (`engine.md` §5, `product.md` §1/§6/Principle 4) and the picker-ranking comments now describe a generic ranked picker over the active library. Convex deploy verified live on prod. (#130)

## 2026-06-16 Tier-3 process changes: /reconcile-ops, feature close-out, canon-to-pointer dedup

Adds the `/reconcile-ops` command and its canonical spec home (`MAINTENANCE.md` §7) for reconciling the operational docs (`README.md`, `CLAUDE.md`, `MAINTENANCE.md`, `ADDING-DISHES.md`, `claude-design.md`) and the `.claude/commands/*.md` briefs against shipped reality, with `last_reconcile_ops` added to `.maintenance-state`. Adds a feature close-out checklist as `docs/development.md` §3 step 8 (archive the feature spec, reset CLAUDE.md "Currently building", run reconciliation) plus a merge-time PR-number backfill note in step 6, and a CLAUDE.md cross-reference. De-duplicates copied canon in the slash commands: `slow-loop.md` and `reconcile-docs.md` now point at the canonical wording in `docs/` and `MAINTENANCE.md` instead of restating it. Docs and command files only; no engine, app, or canonical-data change. (#128)

## 2026-06-16 Documentation-audit corrections (Tier 1+2)

Corrects the `dishDislikes` status across `MAINTENANCE.md` and `slow-loop.md` (the table is a live signal channel; only the dislike write-back mutation remains an open follow-up), fixes the README dish count to roughly 260, de-duplicates the reconcile-docs root allowlist, fixes the slow-loop fixture filenames, completes the engineering §14 data tree, archives `data/menu_images/` and the completed `features/tuhina-feedback.md`, and flags the stale design handoff. Docs only. (#127)

## 2026-06-16 Activate Pav (id 281)

Flips Pav from `active: No` to `active: Yes`, so it enters weekly menu generation as a breakfast Option C plain carb (the category:Bread plain-carb pool per engine.md §3). The live-data coverage snapshot `cov.withPhoto` moves 259 to 260 (Pav carries its photo); no other report snapshot moves. Data plus test only; no engine source, app, rule, or catalog change. (#122)

## 2026-06-16 Make the coriander-garnish clause conditional in the photo prompt

`buildPrompt` in `scripts/generate-dish-photos.mjs` now drops the skeleton's standing coriander-garnish clause ("any garnish is fresh green coriander (cilantro) leaves, never flat-leaf parsley") when a dish's detail line contains "no garnish" (case-insensitive), substituting a positive bare-surface statement so the "coriander" token never reaches the model; otherwise the cue is kept verbatim. FLUX barely honours negations, so a bare bread (Pav) had been rendering spurious coriander even with a "no garnish" detail line; the fix is keyed on the property, not the dish slug, so it generalizes to every non-garnished category (bread, dessert, fruit, plain rice). Savoury dishes are unchanged and existing committed photos are untouched (only future regens differ). Adds a vitest covering both directions and an entrypoint guard that makes the script importable and path-resolving even though the repo path contains a space, plus a note in `data/dish-photos/STYLE.md`. (#121)

## 2026-06-16 Add Pav as a permanent library dish (inactive, photo'd)

Promotes Pav from a Wed-lunch one-off into the permanent library as dish id 281: category:Bread, time:Breakfast, cuisine:Indian, reusing the existing `Pav Bread` ingredient-catalog row, shipped `active: No` (the review gate) with an AI photo. Inactive, so it does not yet enter generation. (#120)

## 2026-06-16 Stop the offline banner flashing on online Menu opens

The offline banner had been flashing on the Menu on normal online opens: it inferred "offline" from an unresolved Convex `getCurrentWeek` query, which is also `undefined` during a normal cold-open load, so the cached week rendered with the banner every time. A new `useIsOffline()` hook keyed off `navigator.onLine` (reactive to the `online`/`offline` events) replaces that inference; the banner, Edit, and Share now follow the real offline signal instead of the query's loading state. Frontend-only; no Convex, engine, or data change. (#119)

## 2026-06-16 Tighten the Menu title/subtitle gap

Reduces the gap under the Menu title by changing `.change-summary { margin-top: 8px }` to `4px`. Cosmetic, Menu-only single-use class; no other surface affected. (#118)

## 2026-06-16 Unify Back/history navigation across the app

The browser/OS Back gesture (and Android hardware Back) now unwinds the user's actual visit order across the whole app: tab switches, the Day editor, and bottom sheets share one history controller (`app/web/src/lib/backStack.ts`) that owns the single popstate listener and the single history-marker discipline. Back from any screen returns to the previously visited screen; with no previous screen it returns to the homepage (Menu tab, no day open); and Back from the homepage is gated behind a "Leave Plantry?" confirm prompt (`ExitConfirmSheet`, reusing the `Sheet` primitive with a `noHistory` opt-out) before leaving. The actual exit is best-effort: a same-document sentinel keeps the at-home Back catchable, and `history.go(-2)` leaves the app where the app was not the first history entry, but is a clean no-op in an installed PWA where no web API can close the tab. The sheet-only controller from #78 (one marker for all stacked sheets, microtask-deferred pop) is generalized into this unified back-stack with its sibling-swap semantics preserved exactly; an `ignoreNextPop` guard suppresses the self-inflicted popstate from the programmatic sheet-marker pop, since the listener now stays armed for the view/home layer underneath. New `app/web/e2e/back-nav.mjs` Playwright suite covers tab unwind, Day-editor exit, sheet close, and the homepage exit prompt. No engine, Convex, or data change. (#111)

## 2026-06-16 Explore count respects the active filter

The Explore tab's subtitle count now reflects the filtered (`visible`) dish count rather than the full feed total, so the number matches what the user actually sees once filters are applied. Frontend-only (`ExploreScreen.tsx`); no Convex, engine, or data change. (#112)

## 2026-06-16 Explore cards top-align photo and title in stretched rows

`.explore-card` changes from `display: block` to `display: flex; flex-direction: column`, so the photo and title pin to the top of grid-stretched cards instead of being vertically centered by the native button layout (which left a gap above cards with a single tag line). Frontend-only (`index.css`); no Convex, engine, or data change. (#113)

## 2026-06-16 Fruit of the day shown and swappable in the day editor

Follow-up to Stream D (Tuhina feedback item 4). The day editor (`DayScreen`) now renders the Fruit of the day as its own section after Lunch (reusing the Menu card's section label and dish row) instead of skipping the `meal: "fruit"` slot, and the fruit dish is tappable and swappable like any breakfast/lunch dish. The swap path is extended end to end: `getSlotAlternatives` builds the fruit slot's candidate pool from Active, in-season, Category=Fruit dishes (the category-based analogue of the meal-time pool) ranked by the existing picker ranking (engine §5); `swapDish` accepts `meal: "fruit"`, validates the picked dish is in the library, Active, in season, and Category=Fruit (new `dish-not-fruit` recoverable reason), and writes the `manualChanges` swap row exactly like other swaps so the slow loop sees it. The frontend `Meal` type widens to include `fruit` while a new `MealTime` type keeps add/delete/one-off gated to breakfast/lunch (scope: swap only, one fruit per day; no add/delete/custom one-off for fruit). The `manualChanges.meal` union widens to include `"fruit"` (additive). No engine.md or product.md edit: the pool branch lives in the Convex query, the §5 ranking is unchanged, and the fruit pool is already specified as Category=Fruit in engine.md §3.3. (#106)

## 2026-06-16 First-class cuisine field + nested Explore filter

Adds a required `cuisine` frontmatter field to every dish (Tuhina feedback item 5, Stream E; absorbs item 9 for Explore). The field is a display/filter value only, never a rule input. A scripted migration (`scripts/migrate-cuisine-field.mjs`) seeds it from the existing cuisine tags across all dishes and removes those tokens from `tags`, leaving only functional tags (HP, complete_meal, complete_carb, fruit), so cuisine has a single source of truth. Dual-tag `greek, mediterranean` resolves to Greek; `oriental` maps to Thai (Singapore noodles pinned to Chinese); untagged dishes and the fruit bowls default to Indian. The field flows through the parser/serializer/schema (byte-identical round-trip), the bake, the frontend dish type, the Explore card cuisine pill (replacing the old tag-decoding), and the dish-photo prompt's cuisine slot (`generate-dish-photos.mjs` now reads the field; photos already generated, so it only affects future regens). The Explore tab gains a compact nested filter that replaces the flat wrapped chip row: a single horizontal row of Easy-to-cook / Healthy quick toggles plus Cuisines and Meal time entries that open multi-select sub-panels (each option with its dish count and an Apply button); filters AND-combine across dimensions and OR within each set. Engine schema/serializer + round-trip tests, the bake, `app/web` Explore filter, `engine.md` §12, `engineering.md` §4, `data/dish-photos/STYLE.md`. (#105)

## 2026-06-16 Add 7 seasonal monsoon fruit dishes (active, photo'd)

Deepens the Fruit of the day pool (#103) with 7 net-new Category=Fruit dishes (ids 274-280): Mango bowl, Litchi bowl, Jamun bowl, Plum bowl, Peach bowl, Pineapple bowl, Pomegranate bowl. Each ships complete (description, recipe, complexity Easy) and active-with-photo, and every one includes Monsoon in its `seasons`, so all 7 are eligible this Monsoon season alongside Seasonal fruit, Banana bowl, and Papaya bowl. Adds 5 new `data/ingredients.md` catalog rows (Jamun, Litchi, Peach, Plum, Pomegranate, all Group=Other); Mango and Pineapple reuse existing rows. Live-data snapshots move: coverage `withPhoto` 252 to 259 (active library 252 to 259, all photo'd) and the Summer Fruit pool 3 to 5 (Mango and Litchi are also Summer). No engine, app, or rule change. (#104)

## 2026-06-15 Fruit of the day, breakfast savoury-only

Decouples fruit from breakfast (Tuhina feedback item 4, Stream D). A standalone Fruit of the day is now picked for every day Mon-Sat (Saturday included): exactly one Active, in-season, Category=Fruit dish, selected longest-unused and rotated across the days, recency-exempt so within-week repeats are allowed when the pool is thin. It sits outside the breakfast/lunch composition and outside the §9 item cap. Breakfast Option A (complete_meal + fruit) is retired, so the Mon/Wed/Fri 2-item breakfast is savoury (Option B or C); a consequence is that complete_meal breakfasts now appear only on the Tue/Thu single-item slot. The engine carries the pick as `fruit?` on each generated day; Convex stores it as a `meal: "fruit"` slot (one dish) on `currentWeek` (additive schema-union widening, but a regenerate is required to write the new fruit rows); the Menu day card and the menu share image render a "Fruit of the day" section; the grocery list aggregates fruit ingredients like any day dish. Engine, Convex schema, frontend, share canvas, `engine.md` §2/§3/§3.3/§4/§9/§12 and `product.md` §2. (#103)

## 2026-06-15 Define "Healthy" from a real protein-to-calorie and fibre rule

Replaces the inert `healthy` filter tag with an engine-derived `healthy` flag (engine.md §11). `data/ingredients.md` gains `Fat /100g` and `Fiber /100g` columns (populated on all 73 macro-relevant catalog rows, blank on aromatics/herbs/Other). `nutrition.ts` now derives per-person fat, fibre, and calories (Atwater 4/4/9) and a `healthy` boolean: at least 25 percent of calories from protein AND at least 3 g of fibre per person, both tunable named constants; a zero-calorie dish (no macro data) is never healthy, so the filter shows no false positives. The coverage report (§11.1) tracks fat and fibre coverage per family. The Explore feed and swap/add pickers read the derived flag via `app/web/src/lib/healthy.ts` (single source of truth; thresholds never re-implemented in TS). 24 of 252 active dishes currently qualify. Engine, spec, tests, catalog, and frontend move together (parity gate). (#102)

## 2026-06-15 Activate the 50 easy-to-cook expansion dishes

Flips the 50 expansion dishes (ids 222-271) from `active: No` to `active: Yes`, so they enter weekly menu generation; they remain `preferred: No`. The active library grows from 200 to 250 dishes. Two live-data snapshot assertions in `engine/test/data/reports.test.ts` move to match: coverage `withPhoto` 200 to 250 (every activated dish carries a photo), and the special-sourcing report gains Vegetable daliya (Bulgur Wheat) and Lentil salad (Parsley). The three intentionally-retired `active: No` dishes (ids < 222) are untouched. Data plus test only; no engine source, app, rule, or catalog change. (#PR)

## 2026-06-15 Add 50 easy-to-cook dishes (inactive, photo'd) for review

Adds 50 new easy-to-cook dishes to the library (ids 222-271), each shipping complete (description, numbered recipe, complexity, and an AI-generated photo) but `active: No` and `preferred: No`, so they stay out of the weekly rotation until Rajat reviews and activates them. The dish files are generated through the engine's real `serializeDishFile` (round-trip-clean), slugs resolve via `slugForDishes` over the whole library, and every ingredient row maps to an existing `data/ingredients.md` catalog row (no new catalog entries). Because the 50 are inactive they enter no generation pool and the coverage/pool reports are unchanged. Photos were generated through `scripts/generate-dish-photos.mjs` with 50 new per-dish `data/dish-photos/details.md` correction lines (the established quality workflow that overrides the FLUX ingredient priors). Activation later is a one-line report bump (see DECISIONS.md). Data-only; no app or engine source touched. (#PR)

## 2026-06-15  Make the passcode gate accept the six-digit household code

The passcode gate was hardcoded to four digits (four dots, a length cap of four, auto-submit at four), but the household passcode `VITE_PLANTRY_PASSCODE` is six digits. A six-digit code could never satisfy the four-digit gate, so login was impossible with the real code: the gate auto-submitted the first four digits, mismatched, and cleared. `PasscodeGate.tsx` now renders six dots, caps entry at six, and auto-submits at the sixth digit; validation against `expected` is unchanged. The dots CSS is flex with a gap and per-dot fixed sizing, so six dots lay out cleanly with no style change. The live Vercel prod env var `VITE_PLANTRY_PASSCODE` must be set to the six-digit household code for the deployed site to accept it (out-of-band owner step). `app/web` only. (#98)

## 2026-06-15 Lock the detail-sheet hero crop to match the Explore card fix

The dish detail-sheet hero (`.detail__photo`) carried the same anti-pattern as #93: a fixed `height: 150px` against a fluid `width: 100%`, so its `object-fit: cover` crop also went near-square on narrow phones and exposed the bright bowl rim and blurred background at the top of the angled-bowl photos. It is replaced with `aspect-ratio: 5 / 2` (2.5:1), chosen to keep the hero near its prior ~150px height at common sheet widths (the sheet panel spans the viewport minus ~18px insets with no app-level max-width, so it renders ~354px wide on a 390px phone, giving ~142px tall) while staying landscape enough to crop past the rim. The empty-photo placeholder shares the base `.detail__photo` class, so it inherits the ratio and keeps its box, as in #93. `app/web` only. (#94)

## 2026-06-15 Lock the Explore card photo crop so narrow phones don't show a rim

Explore-tab dish cards showed a pale band above the food on narrower phones (read as "white space on top"): the bright ceramic bowl rim and blurred background at the top of the angled-bowl dish photos. Root cause: `.explore-card__photo` set a fixed `height: 96px` against a fluid `width: 100%`, so with `object-fit: cover` the crop's aspect ratio tracked the viewport. Wide phones got a ~2:1 strip the food filled, but narrow phones got a near-square box that revealed more vertical extent of the 1024² square source, exposing the rim; the bug was invisible on the desktop and wider devices, which masked it. The fix replaces the fixed height with `aspect-ratio: 16 / 9`, locking the crop to the same tight, food-filled band on every width. The empty-photo placeholder shares the base class, so it inherits the ratio and keeps its box; no separate rule needed. The same fixed-height/fluid-width pattern in the detail-sheet hero is addressed in #94. `app/web` only. (#93)

## 2026-06-15 Identity screen: clearer title, drop the redundant tile sub-line

Two copy/markup tweaks on the identity (welcome) screen (`app/web/src/components/IdentityPicker.tsx`). The screen title changes from the question "Who is this phone?" to the clearer instruction "Please select the user"; the `identity__hint` subtitle ("Edits and comments carry your name") is unchanged. Each selection tile drops its redundant per-tile sub-line "Stored on this phone only" (`identity__option-sub`), so a tile now shows just the avatar and the person's name ("I am Rajat" / "I am Tuhina"); the now-unused `.identity__option-sub` CSS rule is removed (confirmed referenced nowhere else, keeping the stylesheet free of dead selectors for the stylelint gate). No class renames, no behaviour change. `app/web` only. (#92)

## 2026-06-15 Stop clipping the menu share preview on phones

The "Share this week" sheet's menu slide clipped on the right on phones: dish and lunch text ran past the visible card and the right card border was cut off (the exported image was unaffected; only the on-screen preview clipped). `drawMenuShareCanvas` set an inline `width: 360px` and `height` on the menu `<canvas>`, and inline styles override the stylesheet rule `.share__menu-canvas { width: 100%; height: auto }` that is meant to fit the canvas into the `overflow: hidden` slide frame; since each slide is only 82% of the sheet (~280px on a phone), the fixed 360px canvas overflowed its frame and was clipped. The fix removes the inline size so the class governs on-screen display, scaling the canvas to the frame off its backing-store aspect ratio, exactly as the grocery and recipe slides already fit. The exported PNG is provably unchanged: `menuCanvasToBlob` serializes the 3x backing store (`canvas.width`/`canvas.height` attributes), which CSS display size does not affect. `app/web` only. (#88)

## 2026-06-15 Swap-picker search and quick filters reach the full meal-time pool

The Replace/swap picker requested only the top 60 ranked alternatives from `getSlotAlternatives` and ran both its name search and #82's quick-filter chips over that already-truncated slice, so a recently-cooked staple like Roti or Rice (which the picker recency ranking sinks to the bottom of the ~159-dish in-season lunch pool) was unreachable by name even though it is a valid swap. The fix requests the full ranked pool (`limit` 250, above the ~167 active-lunch maximum) and moves the visible-list logic into a pure, unit-tested `swapPickerVisible(pool, query, filters, suggestedCap)` helper in `app/web/src/lib/library.ts`: with no query and no active filter it shows a short top-12 "Suggested for this day" list, and as soon as a query or a filter chip is active it filters the whole pool by name and `dishMatchesFilters`, so search and the chips both reach every meal-time dish. The display cap is now only a suggestion limit, never the search corpus; this also closes the same blind spot in #82's filter chips. Add-a-dish needed no change (it already filters its full client-side `addablePool`). Eight new `app/web/test/library.test.ts` cases cover the helper, including the Roti-at-the-bottom repro and "the suggested cap applies only when query and filters are both empty". `app/web` only. (#85)

## 2026-06-15 Picker rows: drop the duplicate complexity pill

Removes the redundant cook-complexity pill from the dish rows in the two picker bottom-sheets (Add-a-dish and Replace/swap). Each row already shows the complexity in its subtitle meta line (`dishMetaLine`, e.g. "20 min · Easy to cook · Pre prep"), and was additionally rendering a trailing `ComplexityTag` pill that duplicated it and stole horizontal width, forcing longer dish names to wrap early. The `trailing` prop is dropped from the `DishRow` calls in `SwapPickerSheet.tsx` and `AddDishSheet.tsx`; the row body reclaims the freed width with no new CSS (`.dish-row__body` is already `flex: 1; min-width: 0`). The shared `DishRow` component and its `trailing` prop are untouched, so other callers are unaffected. The now-unused imports (`ComplexityTag`, `complexityVariant`, `complexityLabel`) and the dead `.picker__trailing` CSS rule are removed. This deliberately diverges from the design handoff (whose picker rows carry the pill) on Rajat's direct request. `app/web` only. (#86)

## 2026-06-15 Break FLUX's stubborn priors on the ~17 ceiling dishes (white paneer, whole uncut eggs, thick shrikhand)

The prior re-shoot (#80) accepted FLUX's yellow-paneer and patterned-egg-yolk priors as a hard model limitation on the ~17 paneer and egg dishes. This breaks those priors with targeted detail-line strategies in `data/dish-photos/details.md` (no prompt-skeleton or script change): (1) paneer reframed by analogy as "soft WHITE paneer cubes that look like fresh white tofu or white feta, never yellow" (and "crumbled white paneer like fresh white cottage cheese" for the two bhurji dishes) breaks the yellow prior; (2) egg-in-gravy reframed to "whole peeled hard-boiled eggs, kept WHOLE and uncut (smooth glossy white eggs, no cut yolk shown)" sidesteps the fake sunburst-yolk artefact; (3) shrikhand pinned to "very thick dense ... mounded high ... stiff and swirled standing in soft peaks, absolutely not runny"; (4) sprouts-salad pinned to "small sprouted beans each with a short white sprout tail ... clearly sprouted beans, not noodles". The fail-fast probe (paneer-butter-masala, egg-curry, shrikhand) confirmed WHITE paneer, WHOLE uncut clean-white eggs, and THICK mounded shrikhand. The ~17 affected dishes were re-shot, overwriting their `.jpg`s; the white-paneer fix proved seed-sensitive (a few dishes were re-rolled on a known-good seed to land white). sprouts-salad is much improved (now reads as sprouted beans, not the prior noodle-tangle) but remains the weakest of the set; the two turmeric-tinted paneer-bhurji dishes stay yellowish (bhurji is genuinely turmeric-stained, so a white framing fights the real dish). `details.md` stays exactly 200 data lines and `withPhoto` stays 200 (no dish .md change). (#84)

## 2026-06-15 Search pickers: stable height, spacing rhythm, quick filters

Reworks the search experience in the two picker bottom-sheets (Add-a-dish and Replace/swap) per design feedback. The picker panel now holds a stable height while searching: a new `picker` prop on the shared `Sheet` primitive applies `.sheet__panel--picker { height: 92% }`, so the panel no longer shrinks as results filter down (even to a single match or none); the header (title, subtitle, meal chips, search field, filters) stays pinned and the results list absorbs the variation by scrolling inside `.sheet__scroll`. The change is scoped to the pickers, so reason dialogs and the dish-action sheet still size to content. The spacing rhythm is corrected so the subtitle hugs the title (its own 4px margin) while the search field gets clear separation above it, and the section label below hugs the results list (`14px 0 2px`) and sits well clear of the search field and filters. A quick-filter chip row is added directly under the search field in both pickers, mirroring Explore: "Easy to cook" and "Healthy" (the meal dimension is already fixed by the swap slot or the add-a-dish meal chips, so the Breakfast/Lunch chips are dropped as duplicate controls). The filter vocabulary and matching are extracted to a shared `app/web/src/lib/dishFilters.ts` (`DISH_FILTERS`, `PICKER_FILTERS`, `dishMatchesFilters`) now used by Explore and both pickers, so a chip means the same thing everywhere; multi-select is an AND, identical to Explore. Empty-state copy reflects what is actually narrowing the list ("No dish matches your search." / "...your search and filters." / "...those filters."), and the swap picker's section label flips from "Suggested for this day" to "From the library" once any filter is active. `app/web` only. (#82)

## 2026-06-15 Refine photo prompt for realism (matte/anti-gloss, coriander-not-parsley, cfg 3.0) + per-dish detail counters; re-shoot the 135 low-realism dishes

A realism audit scored all 200 dish photos; 135 scored <=3 for AI tells or ingredient errors. Sticking with FLUX, this refines the prompt and adds per-dish ingredient counters, then attempts to re-shoot only those 135. The shared realism skeleton in `buildPrompt()` (`scripts/generate-dish-photos.mjs`) and the `data/dish-photos/STYLE.md` template are rewritten in lockstep to add a matte natural finish with true slightly-muted colours (not glossy, not oversaturated, not plastic-looking, the audit's #1 universal tell) and to force fresh green coriander (cilantro) garnish, never parsley; `cfg_scale` drops from 3.5 to 3.0 to cut the over-styled gloss. The bisect for the coriander cue surfaced a third filter-blocked token, the hyphenated "flat-leaf" (the bare word "parsley" and the unhyphenated "flat leaf" both pass), so the prompt-only filter sanitizer gains a "flat-leaf" -> "flat leaf" rule alongside the existing "fried"/"sweet-salty" rules. The `details.md` lines for the affected <=3 dishes gain explicit ingredient counters (soft WHITE paneer, loose separate basmati grains, thick dal with visible cooked lentils, smooth plain pale-yellow boiled-egg yolk, thin flat flatbreads not naan) plus bespoke per-dish corrections (bread-omelette, capsicum-aloo, korean-japchae glass noodles, sabudana-khichdi cooked pearls, shrikhand thick, and others). The five fail-fast probes (paneer-butter-masala, jeera-rice, dal-makhani, egg-curry, methi-paratha) confirmed clear wins on loose rice grains, textured dal, flat paratha and reduced gloss, but white paneer and smooth egg yolk did NOT improve over the current versions even with a front-loaded emphatic counter (FLUX's yellow-paneer and textured-yolk priors hold). With those two priors accepted as a FLUX limitation, Rajat approved running the full re-shoot anyway: all 135 low-realism dishes are regenerated with the refined prompt (matte/anti-gloss, coriander-not-parsley, cfg 3.0, per-dish counters), overwriting their `.jpg`s in place; the other 65 dishes are untouched. Paneer colour (renders yellow rather than white) and egg-yolk texture remain a FLUX limitation on the ~16 paneer and egg dishes in the set. `withPhoto` stays 200 (no dish .md change). (#80)

## 2026-06-15 Render the menu share image on a canvas to stop iOS Safari overlap

The weekly menu SHARE image overlapped on real iOS Safari: a breakfast dish list that wrapped to a second line collided with the LUNCH row below it. The root cause is `html-to-image`'s SVG `foreignObject`, which freezes each element's computed pixel height and then re-wraps the text with the embedded webfont; on iOS Safari it re-wraps to more lines than the frozen height allows, so the overflow spills onto the next meal (two prior CSS fixes in #75 could not beat a reflow the pipeline itself triggers). A new `app/web/src/components/menuShareCanvas.ts` renders the menu on an HTML `<canvas>` with a layout pass (`measureText` plus manual line-breaking, hard-breaking any single over-long token) followed by a draw pass, so every y-position derives from the actual measured line count and overlap is structurally impossible. The same canvas backs both the on-screen preview slide and the exported PNG, so they cannot drift: `SharePreviewSheet` shows the canvas and reads `canvas.toBlob()`, and `ensureMenuShareFonts()` awaits every exact face/weight/size before any measure or draw so wrap points are stable. Grocery and recipe sheets stay on `html-to-image` (they have not shown the bug); `MenuShareImage` is removed and `buildShareDayModels`/`ShareDayModel` are exported so the canvas reads the same data model as the engine. `app/web` only. (#81)

## 2026-06-15 Menu and Explore UI design-feedback fixes

Applies eight design-feedback items across the Day editor, swap picker, dish-detail, and Explore sheets, plus a browser-Back fix for sheets. Removes the day-level and dish-level comment-entry affordances (the queued-comments backend and the Changes feed are untouched; `CommentSheet` is deleted); stops the sheets' full-width inputs (search field, reason textarea) from clipping their side border and focus outline by splitting the panel's horizontal inset into 12px panel plus 6px scroll box; adds the handoff's 14px gap between the swap search field and the "Suggested for this day" label; and reworks the replace flow so picking a library dish first shows that dish's details (the new shared `DishDetailBody`, also adopted by the Menu and Explore detail sheets) with a primary "Replace dish" action and an optional reason (`swapDish` now accepts an empty reason). A browser Back now closes the open bottom sheet instead of leaving the app: the `Sheet` primitive's per-sheet history markers are replaced by one module-level controller that keeps a single history entry while any sheet is open, defers its pop via `queueMicrotask` so sibling sheet-swaps do not spuriously fire Back, and keeps a closer stack so Back closes the top sheet. The Explore "Not for me" control reads as tappable (dashed underline, 44px tap target), the "Include recipe when sharing" toggle is surfaced at the page level of the Menu dish-details sheet (and left off Explore, where an unplaced dish has no slot to write to), and sheets now move focus into the panel on open. The first cut of the Back model had a React cleanup-before-setup race that self-closed every sheet-to-sheet transition; the per-slice crawl gate caught it before merge and it was fixed in the same PR. (#78)

## 2026-06-15 Dish photos regenerated with per-dish visual details (realism skeleton + details.md)

Re-shoots the entire dish-photo library so each dish renders its true ingredients. The prior single generic realism prompt held the overall look but produced ingredient-level errors (okra as whole cylinders rather than sliced rounds, plain roti garnished with coriander, boiled eggs too smooth, dry dishes shown in sauce). The fix is per-dish: a new committed detail map, `data/dish-photos/details.md`, carries one visual-detail line per dish (form, cut, garnish, dry-vs-gravy state, texture), derived from a per-dish study of real reference photos plus culinary knowledge; `buildPrompt()` in `scripts/generate-dish-photos.mjs` and the `data/dish-photos/STYLE.md` prompt template are rewritten in lockstep to a shared realism skeleton with three slots (`{dish name}`, `{cuisine}`, `{per-dish detail}`), injecting each dish's line into the skeleton. A missing slug falls back to the dish file's first-paragraph description. Generation keeps `cfg_scale` 3.5, `steps` 40, 1024² square, the per-dish slug seed, the concurrency pool plus rate limiter plus backoff, and the prompt-only filter-token sanitizer ("fried", "sweet-salty"). All **200 of 200** active dishes are force-regenerated, overwriting the prior set; the flagged dishes now render correct ingredients (sliced okra, halved eggs, plain roti, dry chilla, loose-grain rice). `withPhoto` stays 200 (dish files unchanged). Supersedes the prior 200/200 photo set (#72). (#77)

## 2026-06-15 Explore cards show a multi-tag set (concise difficulty + prep + descriptor)

Each Explore dish card now renders a wrapping pill set instead of one verbose complexity pill, surfacing the details that help someone choose a dish. A pure helper `exploreCardTags(dish)` in `app/web/src/lib/library.ts` maps a `Dish` to an ordered tag list: a concise, colored difficulty pill (`Easy`/`Medium`/`Hard` via new `complexityShortLabel`) always first, a `{prepMinutes} min` pill when prep time is present, and one descriptor by priority (High protein for the `HP` tag, then Complete meal for the category or `complete_meal` tag, then a title-cased cuisine tag, then Filling for High satiety). Dishes with empty `tags` degrade gracefully and never leak a raw code or an empty pill. A neutral `MetaTag` primitive and `.meta-tag` / `.explore-card__tags` CSS render the non-difficulty pills in a quiet style, capped at two lines. New `app/web/test/library.test.ts` covers the helper (vitest wired via `app/web/vitest.config.ts` + `test`/`pretest` scripts). `app/web` only. (#76)

## 2026-06-15 Bottom nav: distinct line icons per tab

Replaces the bottom nav's single 5px placeholder dot with a distinct minimal line icon per tab: Menu (calendar), Grocery (basket), Explore (compass), Changes (swap arrows). Icons are inline SVGs in `primitives.tsx` (a type-checked `Record<TabKey, ReactNode>` map, no icon library) that inherit the tab button's color via `currentColor`, so they pick up the terracotta accent when active and the muted sub color when inactive with no per-icon color CSS; `.tab-bar__dot` is replaced by a sizing-only `.tab-bar__icon` rule. The design handoff's TabBar uses the same placeholder dot, so the icons are a deliberate, EM-authorized step beyond the handoff (see DECISIONS). Verified by the per-slice full-flow crawl across all four tabs: no overflow, 4 legible icons per tab, active=accent bold, 47px tap targets, clean console. (#74)

## 2026-06-15 All dish photos regenerated again for photorealism (realism prompt v2)

Re-shoots the entire dish-photo library a second time for a more honest, real-looking result. The prompt in `data/dish-photos/STYLE.md` and the script's `buildPrompt()` are rewritten in lockstep from a study of 60 real reference photos against the prior AI set, which read as too clean and styled. The prior overhead, plain-plate, centred-mound framing is replaced with candid realism: a natural angle (low or three-quarter, not flat overhead) with a shallow depth of field, a real everyday vessel that suits the dish (a steel katori or thali, a karahi or kadai, a cast-iron pan, or a plain home plate), a softly out-of-focus home or restaurant background, ordinary warm light, and honest true-to-life colour. The decisive cue is food that looks genuinely cooked and a little imperfect with the correct real texture per type (a dry dish stays dry, a gravy is thick, opaque, and oil-flecked, rice is loose separate grains, a pudding is loose and soft), irregular hand-made shapes, uneven edges, and scattered garnish. Generation keeps `cfg_scale` 3.5, `steps` 40, 1024² square, the concurrency pool plus rate limiter plus backoff, and the prompt-only filter-token sanitizer ("fried", "sweet-salty"); it now gives each dish a base seed derived from its slug so vessels, backgrounds, and angles vary across the library (variety is now desired, not uniformity) while a re-run reproduces the same set. All **200 of 200** active dishes are force-regenerated, overwriting the prior set; `withPhoto` stays 200 (same dishes). Supersedes the prior 200/200 photo set. (#72)

## 2026-06-15 Fix: Explore + Share restored (unbalanced CSS) + CSS lint gate

Restores two broken surfaces and adds a CI gate so they cannot silently break again. `app/web/src/index.css` had three missing closing braces (`.change-entry__meta`, `.change-entry__note`, `.toggle--on .toggle__knob`) that, under native CSS nesting, silently swallowed every rule after them: the Explore tab rendered a single overflowing 1024px image and the Share images rendered as unstyled text. The braces are balanced, several tap targets are raised to 44 px, and a new stylelint **"Lint CSS"** CI gate (`.stylelintrc.json`, `lint:css`, `ci.yml`) fails the build on unbalanced or unclosed CSS — proven to catch this exact bug. An optional local Playwright render smoke test (`app/web/e2e/smoke.mjs`) ships alongside. Surfaced by a live prod UI/UX audit (see `DECISIONS.md`). (#69)

## 2026-06-15 Fix: edit-flow interaction polish

Tightens the live edit flows surfaced by the same prod audit. The ReasonDialog submit button is now truly disabled until a reason is entered (it was clickable-but-silent across Replace / Add / Delete / Skip / Restore); the Swap picker shows a "No dish matches that name." empty state; the Sheet primitive locks background scroll; pickers and comment fields autofocus and carry aria-labels; and the dish-removal verb is now consistently "Delete" across the action menu, reason-dialog title and hints, the detail button, the changelog headline, and the summary count. `app/web` only. (#68)

## 2026-06-15 All dish photos regenerated for photorealism (coverage 200 of 200 active)

Replaces the entire dish-photo library with a more realistic look. The prior photos read as glossy CGI/illustration; the prompt in `data/dish-photos/STYLE.md` is rewritten from a styled matte-stoneware flat-lay (with a corner prop) to a candid home phone photo: a single helping on a plain everyday plate or bowl on an ordinary kitchen surface, shot straight down in soft window light, true-to-life colours, real and slightly uneven textures, deliberately unstyled. Generation parameters move to `cfg_scale` 3.5 and `steps` 40 (lower guidance plus more steps render looser, more natural food rather than plasticky CGI), 1024² square. The cuisine slot is now a bare adjective ("a dish of {cuisine} home cooking"), dropping the old article and "home-cooked dish" wrapping. `scripts/generate-dish-photos.mjs` keeps the prompt-only "fried"-token sanitizer (NVIDIA's FLUX.1-dev content filter false-positives on the literal "fried"; the assembler rewrites it to a visually-equivalent synonym, never touching the dish file) and adds parallel generation: a bounded concurrency pool (default 6 in flight) gated by a client-side ~35 req/min rate limiter (under NVIDIA's ~40/min free-tier cap), with exponential backoff plus jitter on HTTP 429/500/503. A new `--all`/`--force` flag force-regenerates every active dish, overwriting existing photos. The run re-shoots all **200 of 200** active dishes; every dish now carries a `photo:` field. The `reports.test.ts` `withPhoto` live-data snapshot moves 191 → 200. Supersedes #65 (the 9 old-prompt dishes are regenerated here with the new prompt). (#66)

## 2026-06-15 Dish-photo run resumed on NVIDIA FLUX.1-dev (coverage 191 of 200 active)

Resumes the dish-photo track on a new provider after the Hugging Face free-tier monthly credits depleted. `scripts/generate-dish-photos.mjs` is repointed to FLUX.1-dev via NVIDIA NIM (`ai.api.nvidia.com/v1/genai/black-forest-labs/flux.1-dev`), a higher-fidelity full (non-distilled) FLUX model than the prior FLUX.1-schnell; the key is read from `NVIDIA_API_KEY` and the HF path stays in the tool as a dormant fallback (`PROVIDER=hf`). Everything downstream is unchanged: the same cuisine-aware positive-constraint prompt from `data/dish-photos/STYLE.md`, the same `sips` sizing to a square 1024² JPEG under ~300 KB, the same `photo:`-frontmatter write and resumability. The run lands 158 new images over the remaining active dishes in slug-alphabetical order, bringing active-dish photo coverage to **191 of 200**. Nine dishes the NVIDIA safety filter declines to render (it returns a black frame with `finishReason: CONTENT_FILTERED`, mostly rice-heavy dishes such as the biryanis) stay on the no-photo placeholder; the tool now detects that signal (plus a luminance guard) and skips such dishes cleanly rather than writing a black image. The `reports.test.ts` `withPhoto` live-data snapshot moves 33 → 191. (#64)

## 2026-06-14 Slow-loop: one-HP-per-meal (all forms) + protein diversity

Generalizes the one-HP-per-meal rule across every `docs/engine.md` §3 menu form (breakfast pair, breakfast single, Menu 1-4) so a single meal carries at most one HP-tagged dish: each form picks its protein main first, and once an HP dish occupies the meal the remaining non-main positions exclude HP-tagged dishes (keyed on the `HP` tag, never on names; the same thin-pool fallback fills the slot with the unfiltered pool when excluding HP would empty it). It also adds §4 step 6 within-week protein diversity for HP mains: when ranking an HP-main pool, a candidate whose protein family already appeared as an HP main earlier in the week sinks below fresh-protein candidates, so the week's HP mains spread across proteins (fish, prawn, mutton, egg) rather than repeating chicken or paneer. A new §4.6 protein-family normalization keys the comparison (Chicken, Chicken Breast, Chicken Keema collapse to Chicken; the soya forms collapse to Soyabean Chunk; every other protein maps to itself). The diversity step is a soft preference with the fresh-alternative fallback (it never narrows §3 eligibility, never overrides the recency exemptions, and acts only on HP-main pools). A Rajat-directed follow-up to the same day's #61 run after verifying the regenerated 2026-06-15 week; no Convex rows were consumed. The simulation harness now asserts no single protein is the HP main three or more times in a week. (#62)

## 2026-06-14 Slow-loop: within-week recency + one-HP-per-meal (Menu 1)

Two engine rule changes from a Rajat-directed proactive run after a menu review of the 2026-06-15 generation, where the same HP gravy won every Mon/Wed/Fri Menu 1 slot and a Menu 1 HP main sat beside a second HP dish in one meal. `docs/engine.md` §4 gains within-week recency (step 5): a dish placed in an earlier slot of the week being generated is treated as most-recently-used for every subsequent slot, so it sinks below any fresh alternative; it is applied last so neither ingredient consolidation nor Preferred=Yes can re-promote an already-placed dish, and it honors the existing fruit and lunch-carb exemptions (Seasonal fruit on Mon/Wed/Fri and a repeating roti stay intended). §3 Menu 1 partner selection now excludes HP-tagged dishes from the partner pool when the main is the HP pick (one HP source per meal), keyed on the `HP` tag with a thin-pool fallback so the slot still fills if excluding HP empties the pool. No Convex rows were consumed (every consumed-id list is empty). (#61)

## 2026-06-14 Mutton HP expansion batch (5 dishes + Mutton catalog ingredient)

The fifth expansion batch (`data/expansion-4`, slow-loop Cluster C content batch, Rajat-reviewed), adding 5 mutton high-protein dishes (ids 217-221) that open the red-meat axis the library lacked entirely: **Mutton rogan josh** and **Mutton curry** (Gravy dish, deepening the Menu-1 HP-gravy pool that drove the repetition), **Mutton keema** (Dry dish), **Mutton pepper fry** (Keto), and **Mutton biryani** (Complete meal, `complete_meal`+HP). Each ships complete (full frontmatter, one-line description, catalog-resolving ingredients, serves-two recipe; no photo, deferred on HF quota) with realistic long mutton prep times (45-60 min) and Medium/Hard complexity. One new catalog ingredient lands, **Mutton** (Proteins and Dairy, unit g, untracked by weight like Chicken, macros 25/0 per 100g cooked, regular Bangalore sourcing so `Special` blank), mirroring how Chicken is cataloged; every dish otherwise reuses existing aromatics, curd, and peas. The library grows from 198 to 203 dishes; the Menu-1 HP gravy pool deepens by two; the 5-week simulation and all validators stay green. (#60)

## 2026-06-14 Dish-photo full run (coverage 33 of 195 active)

The dish-photo track's full run. The generation pipeline shipped in the #58 pilot (`scripts/generate-dish-photos.mjs` + `data/dish-photos/STYLE.md`): FLUX.1-schnell via Hugging Face Inference Providers, a cuisine-aware **positive-constraint** prompt (the frame names only the allowed objects, the dish and at most one corner prop, rather than listing forbidden cutlery — a guidance-distilled model honors that far better), and macOS `sips` sizing to a square 1024² JPEG under ~300 KB with no npm image library. This batch runs that pipeline over every active dish that lacked a photo, in slug-alphabetical order, and lands 18 new images, bringing active-dish photo coverage to **33 of 195**. The run stopped on a Hugging Face HTTP 402 (free-tier monthly inference credits depleted), so the remaining ~162 active dishes stay photoless for now; the PWA's no-photo placeholder (the `Thumb` diagonal-stripe fallback) keeps partial coverage looking intentional, and a later run picks up where this one left off (the pipeline is resumable — it skips any dish that already carries a `photo:` field). The `reports.test.ts` `withPhoto` live-data snapshot moves 15 → 33. The look was approved via the pilot, so this is a spot-check review. (#59)

## 2026-06-14 Dish-photo generation pipeline + pilot

The offline build-time dish-photo pipeline. `scripts/generate-dish-photos.mjs` reads the photo style spec `data/dish-photos/STYLE.md`, a cuisine-aware **positive-constraint** prompt: the prompt names only the allowed objects (the dish and at most one corner prop) rather than listing forbidden cutlery, which a guidance-distilled model honors far better, with the cuisine derived per dish from its tag (Indian default). Images come from FLUX.1-schnell via the Hugging Face Inference Providers router (reads `HF_TOKEN` from the environment) and are normalized with macOS `sips` to a square 1024² JPEG under ~300 KB, so there is no npm image dependency. The tool writes `data/dish-photos/<slug>.jpg` and sets the dish's `photo:` frontmatter; it is resumable, skipping any dish that already carries a `photo:` field. The 15-dish pilot validated the look across cuisines and categories. (#58)

## 2026-06-14 Slice 10.1: design-revamp close-out (product.md rewrite + reconcile + archive)

Closes the design revamp. `docs/product.md` is fully rewritten to the shipped product as present-tense steady state (no historical seams): the four-tab PWA (Menu, Grocery, Explore, Changes), the full editing family (swap with picker ranking, add a library dish, custom one-off via replace, delete, skip/restore a day, comments), the Explore feed (familiar-but-new ranking, filters, use-this-week/next-week, a records-only dislike read only by the slow loop), the Changes feed, and the client-side share image family (menu/grocery/recipe via Web Share) that replaces the single PNG; scope folds in the shipped pulls (day skipping, add/delete, the next-week queue, ~200 dishes across ~10 cuisines, special-sourcing), and future scope holds photo-coverage completion, the Swiggy ordering automation, calendar awareness, and the deliberately-unbuilt append-custom-dish. The other three canonical docs are reconciled against the CHANGELOG run since 2026-06-10: `engine.md` §8 and `development.md` §1/§4 lose stale "will be built"/`phase2.md` references, and `engineering.md` §3 gains the shipped `dishDislikes` table and lists it among the slow-loop signal channels. `features/design-revamp.md` moves to `archive/features/`, `CLAUDE.md`'s "Currently building" returns to "_none_", and `.maintenance-state` advances `last_reconcile` to 2026-06-14. The revamp is complete: engine, backend, data, the four-tab PWA, the slow-loop upgrade, the share family, and the dislike signal are all live. (#57)

## 2026-06-13 Slice 7.1: Explore tab + dislike signal

Builds the Explore tab and lands the dislike signal end to end. `ExploreScreen` subscribes to `getExploreFeed` (now extended to also read `currentWeek` and `nextWeekQueue` so dishes already placed this week or queued for next are hidden server-side, Decision #9) with multi-select filters (Easy to cook, Healthy, Breakfast, Lunch) over a two-column card grid with the no-photo fallback. The dish sheet re-renders the 5.2 detail surface with the recipe visible plus a plain "why it fits" line derived from the dish's `dominantAffinity` key (no internal labels, Principle 7). "Use this week" picks a day and calls `addDish` (through the shared reason dialog); "Next week" calls `saveForNextWeek` (reason required, Decision #8). The **dislike** feature ships: a new additive `dishDislikes` table (`{ createdAt, author, dishId, reason: string | null, status, consumedWeekStart }`, indexed `by_status`, parallel to `nextWeekQueue`, brand-new so row-safe with no wipe) and a camelCase `dislikeDish` mutation that writes one `dishDislikes` row (reason optional, no `manualChanges` row, no week mutation). A "Not for me" affordance on the dish sheet records the signal and does nothing in-session, neither re-ranking nor hiding (Principle 5, Decision #12); it is consumed only downstream by the slow loop (9.1), which now has live `dishDislikes` data. The Healthy filter is forward-compatible (no dish carries a `healthy` tag yet). Explore stub retires. (#56)

## 2026-06-13 Slice 8.1: share image family + Web Share

Replaces the single weekly PNG with a client-side share-image family rendered from React via `html-to-image` (the approved new library, Decision #5). Three image types in `ShareImages.tsx` (a label-free cream surface, Principle 7): a **menu** image (one card per day, skipped days shown as "Skipped"), a **grocery** image (the week's buy list from the same skip-aware `getGroceryList`, so it matches the Grocery tab), and a **recipe sheet** per dish marked "include recipe when sharing" (de-duplicated by id, degrading gracefully when cook fields are absent). The Menu's "Share this week" button opens a swipe-rail preview (`SharePreviewSheet.tsx`); each slide renders to PNG at 3x and is delivered via the Web Share API level 2 (files) when available (installed PWA to the native share sheet to WhatsApp), with download-all as the fallback. The per-dish "include recipe when sharing" toggle wires to the existing `setIncludeRecipe` (a share preference, no `manualChanges` row, resets weekly per Decision #10). The old single-PNG path had no runtime code (only spec text), so retiring it is a docs rewrite: `docs/engineering.md` §12 and `docs/product.md` §3 move to the client-side image-family model and `data/menu_images/` becomes archive-only. `app/web` only (plus the two docs); no Convex or engine change. (#55)

## 2026-06-13 Slice 6.1: Changes tab + Menu summary line

Builds the Changes tab. A new `ChangesScreen` renders a newest-first feed merging two live Convex subscriptions, the week's `manualChanges` (via 4.2's `listManualChangesForWeek`) and the comments (filtered client-side to the current week), so every edit and comment appears with its author, absolute timestamp, plain-language headline, and quoted reason/comment text. All `manualChanges` kinds render (swap, custom, add, delete, skip_day, restore_day, save_next_week) plus dish-level and day-level comments; dish ids resolve to names via the baked library and no internal enum value reaches the screen (Principle 7). The Menu screen's change-summary placeholder (from 5.1) wires to real data through `deriveSummaryLine` (e.g. "3 swaps, 1 skip this week"; "No changes this week yet" when empty), counting menu edits only (comments are not edits). The Changes stub retires; `app/web` only, no Convex/engine/data changes. (#54)

## 2026-06-13 Slice 5.2: PWA editing family + legacy retirement

Turns the read-only shell into the interactive app. Editing is entered from a day card's Edit button into a **Day screen** (day-level or dish-level only; meal blocks are never edited as a unit), which wires the slice-4.1 mutations and 4.2 queries: a **details-and-recipe sheet** (photo/fallback, description, ingredients, cook fields, recipe, dish-level comments); an **action sheet** (Details / Replace / Delete); a **swap picker** using `getSlotAlternatives` (the slice-3.1 picker ranking, non-restrictive pool) into `swapDish`; **add a dish** (`addDish`, library dishes into the day); **delete** (`deleteDish`, a day may drop below its composition shape, Decision #11); **skip / restore** the day (`skipDay` / `restoreDay`, lossless); and a shared **reason dialog** with quick chips reused across every reason-required write (Decision #8). Comments land both dish-level (in the details sheet) and day-level ("Comment on this day"), via `addComment`. The custom one-off lives in the Replace flow (the only one-off mutation `addCustomOneOff` replaces a position; appending a custom dish would write a misleading `add` signal, so it is deliberately not composed — an append-custom mutation is a tracked follow-up). The legacy `CurrentWeekView`, `SlotEditor`, `CommentComposer`, `CommentsList`, `LegacyEditScreen`, and the orphaned `GroceryList` are removed. Convex subscriptions and optimistic concurrency are unchanged; no internal labels leak (Principle 7). `product.md` §2/§6/§7 get minimal lockstep edits as day skipping and delete enter product scope (the full rewrite is slice 10.1). (#53)

## 2026-06-13 Slice 5.1: PWA shell (tokens, primitives, Menu + Grocery read-only)

The PWA rebuild begins. The `design_handoff/hifi-tokens.jsx` design tokens port verbatim into `app/web/src/index.css`, and the handoff primitives become real React + CSS: day card, dish row, sheet, chips, avatar, buttons, and a four-tab bar (Menu, Grocery, Explore, Changes), including the `Thumb` no-photo fallback (a quiet diagonal-stripe placeholder, so the zero-photo library today reads as intentional rather than broken). The passcode gate and identity picker are restyled to the new look with unchanged behaviour. The **Menu** screen renders the current week as day cards with dish rows (photo or fallback, pre-prep markers, plain-language complexity), a week header with the identity avatar, a change-summary placeholder, and skipped-day rendering; the **Grocery** screen renders the grouped, skip-aware `getGroceryList`. Explore and Changes are placeholder stubs (built in 7.1 and 6.1). This slice is read-only: editing still routes through the retained legacy `SlotEditor` (the redesigned editing family and the retirement of the old components are slice 5.2). Convex subscriptions and optimistic concurrency are unchanged, and no internal labels leak to the UI (Principle 7). `app/web` only; no Convex, engine, or data changes. (#52)

## 2026-06-12 Slice 9.1: slow-loop upgrade (new signals + reports + mark-applied)

The slow loop's value upgrade, so it learns from every signal the revamp produces. `MAINTENANCE.md` and `.claude/commands/slow-loop.md` gain: the coverage and pool-coverage reports plus `nextWeekQueue` (and `dishDislikes`) as inputs, with `npm run reports` read on every run; clustering guidance for the six new signal channels (skips, deletes, adds, saves, unplaceable requests, dislikes); a right-size examples table rewritten for the per-dish-file structure; and a new "proactive runs" subsection, so even a zero-comment week can produce a useful PR from the reports (e.g. a thin seasonal pool). Mark-applied closes the loop for the queue: PR cluster blocks gain a `next_week_queue_ids:` key, a new internal camelCase mutation `nextWeekQueueMutations.ts:markQueueDropped` marks stale `queued` rows `dropped` (never-throw, warn-incident discipline; it deliberately spares `placed` rows so a real generation wins over a stale drop), and `scripts/slow-loop-mark-applied.mjs` parses the new key (backward-compatible with older bodies). Dislikes are documented and fixtured but their write-back is a tracked slice-7.1 follow-up since the `dishDislikes` table is built there; the script parses `dislike_ids:` tolerantly and logs them without writing. New fixtures cover the new `manualChanges` kinds, a `nextWeekQueue` row, and a `dishDislikes` row; the upgraded loop was dry-run against them. (#51)

## 2026-06-12 Slice B3.4: library expansion batch 4 (19 dishes — rounds out world cuisines + thin pools)

The fourth expansion batch (`data/expansion-3`, Rajat-requested and -reviewed), adding 19 dishes (ids 198-216, Easy/Medium only) that round the newer world cuisines into real rotations and deepen thin pools: Mexican (chicken enchilada bowl, mexican rice, guacamole), Spanish (new cuisine: spanish omelette, patatas bravas, spanish chickpea spinach stew, gazpacho), Continental (ratatouille, continental baked vegetables), Korean (japchae, tofu soup), Japanese (teriyaki chicken, miso soup), Vietnamese (new cuisine: noodle salad, lemongrass chicken), Greek/Mediterranean (lemon chicken and potatoes, tzatziki), and two desserts (Greek yogurt with honey and walnuts, mango sorbet) for the thinnest pool. Each ships complete with a cuisine tag and `HP` where genuine, mapped onto the existing category enum (no new category; soups map to Accompaniment or Gravy dish per weight, a cold noodle salad to Complete meal). Seven new catalog ingredients land with macros (avocado, brinjal, honey, lemongrass, miso paste, sesame oil, zucchini); only miso paste is flagged `Special`. The library grows from 179 to 198 dishes; the 5-week simulation stays valid. (#49)

## 2026-06-12 Slice 4.2: Convex feed/explore/grocery queries + queue consumption + skip-exclusion

Wires the slice-3.x engine rules into Convex, all additive and behaviour-identical in production until the PWA starts writing skips and queue rows. Two new queries: `queries/activity.ts:listManualChangesForWeek` (the week's `manualChanges` rows; the client merges the existing `comments` query for the Changes tab) and `explore.ts:getExploreFeed` (the engine `explore.ts` ranking joined with archive-derived last-cooked, each dish carrying its `dominantAffinity` key for the Explore tab). `getGroceryList` becomes skip-aware: it groups picks per day and passes `currentWeek.skippedDays` to the engine aggregator so skipped days drop out, while items keep `ingredient, quantity, unit, tracked, packs, packTotalGrams` (product.md §8 invariant 2). `generateCurrentWeek` consumes `queued` `nextWeekQueue` rows as the engine `requests` input (slice 3.2): rows the engine places are marked `placed` with the week start, rows it cannot place stay `queued` with the engine incident persisted (placed-exactly-once XOR incident). A new minimal `finalizeWeek` mutation (the long-deferred Stream C follow-up, with author/version/already-final guards) appends a `weekArchive` row that excludes skipped days and custom one-offs, so recency never sees uncooked dishes. With no skipped days and an empty queue (production today) grocery, generation, and archive are byte-for-byte unchanged, and the current frontend is untouched. No schema changes (4.1 did them); all `app/convex/` filenames camelCase. `docs/engineering.md` §5 updated. (#50)

## 2026-06-12 Slice B3.3: library expansion batch 3 (20 international + world-cuisine dishes)

The third expansion batch (`data/expansion-2`, Rajat-requested and -reviewed), adding 20 dishes (ids 178-197, Easy/Medium only): 8 more across the existing four cuisines (Thai tofu stir fry, Thai pineapple fried rice, Chinese garlic noodles, Chinese chilli garlic prawns, pasta primavera, baked mozzarella pasta, muhammara, Lebanese lentil soup) and 12 across five new world cuisines — Mexican (bean burrito bowl, chicken fajita bowl, black bean quesadilla), Continental (mushroom risotto, continental grilled chicken), Korean (tofu bibimbap, korean chicken stir fry), Japanese (teriyaki tofu rice, japanese egg fried rice), and Mediterranean/Greek (greek salad, mediterranean couscous bowl, greek chicken souvlaki). Each ships complete (full frontmatter, description, catalog-resolving ingredients, serves-two recipe; no photo) with a plain cuisine tag and `HP` where genuinely high-protein, mapped onto the existing category enum (no new category; a soup maps to Gravy dish, grilled skewers to Dry dish). Eight new catalog ingredients land with macros (couscous, feta, gochujang, pineapple, pomegranate molasses, soy sauce, tortilla, walnut); two are flagged `Special` (gochujang and pomegranate molasses, both specialty-store in Bangalore), the rest regular. The special-sourcing report's live-data guard widens to the new special set. The library grows from 159 to 179 dishes across roughly ten cuisines; the 5-week simulation stays valid. (#48)

## 2026-06-12 Special-sourcing catalog column + dish eval (+ parsley)

Adds sourcing metadata to the ingredient catalog and a report that flags which dishes need a specialty-store run (Rajat request; the additive sourcing column `features/design-revamp.md` §1.1 anticipated for the future Swiggy ordering automation, product.md §8). `data/ingredients.md` gains a `Special` column (`Yes` or blank; only those values validate) carried through the parser, serializer, and the `CatalogIngredient` schema (`special: boolean`), round-trip byte-identical. A new `Parsley` row lands (Aromatics and Herbs, with macros). The `Special = Yes` set is exactly three genuinely-hard-to-source-in-Bangalore ingredients — **parsley, tahini, bulgur wheat** — after Rajat confirmed the borderline items (and even mozzarella, tofu, olive oil) are easily available locally. `Tabbouleh` switches its `Coriander Leaf` row to `Parsley`, matching its description. A non-blocking `specialSourcingReport()` (in `engine/src/data/validators.ts`, surfaced by `npm run reports`) lists, per active dish, the special-sourcing ingredients it uses: today only Hummus (tahini) and Tabbouleh (bulgur wheat + parsley); every Indian staple dish is correctly silent. `docs/engine.md` field reference and reports subsection updated; `features/design-revamp.md` §1.1/§1.3 + decision 13 and a `DECISIONS.md` entry record the feature. (#47)

## 2026-06-12 Slice B3.2: library expansion batch 2 (18 international dishes)

The second expansion batch (`data/expansion-1`, Rajat-requested and -reviewed), adding 18 international dishes (ids 160-177, Easy/Medium only) across four cuisines, each complete on arrival (full frontmatter, description, catalog-resolving ingredients, serves-two recipe; no photo) and carrying a plain cuisine tag plus `HP` where genuinely high-protein. Oriental/Thai (5): thai red curry tofu, thai green curry chicken, pad thai prawn, thai basil chicken, singapore noodles. Chinese (4): veg hakka noodles, chicken fried rice, veg manchurian gravy, chilli chicken dry. Italian (5): penne arrabbiata, pasta pomodoro, spaghetti aglio e olio, pesto pasta, caprese salad. Lebanese (4): hummus, falafel, tabbouleh, shakshuka. Mapped onto the existing category enum (no new category invented; pasta/noodle bowls → Complete meal, stir-fries → Dry dish, curries → Gravy dish, dips/salads → Accompaniment). 12 new catalog ingredients land with macros (basil, bean sprout, bulgur wheat, cornflour, mozzarella, noodles, olive oil, pasta, spaghetti, spring onion, tahini, tofu; mozzarella and tofu tracked with pack sizes). The library grows from 141 to 159 dishes; the 5-week simulation stays valid. (#46)

## 2026-06-12 Plan: Explore "dislike" signal woven into the design revamp

Planning amendment (no code): incorporates Rajat's requested Explore-tab "dislike" affordance into `features/design-revamp.md` so it ships with the slices that own it. A dislike records a slow-loop signal and does nothing in-session (Principle 5, record do not apply). It is stored in a new additive `dishDislikes` Convex table plus a `dislikeDish` mutation (built in slice 7.1, deliberately not a `manualChanges` kind), read and clustered by the slow loop in slice 9.1 (one dislike is no change; a dish disliked repeatedly or by both household members is a deactivation/down-rank candidate). EM design defaults logged in the §3 decisions table (row 12) and `DECISIONS.md`: reason optional, no in-session effect, no auto-hide; all reversible until 7.1 ships. (#44)

## 2026-06-12 Slice B1.3: enrichment batch 3 (30 dishes) — recipe coverage complete

The third and final enrichment batch (`data/enrichment-3`, Rajat-reviewed for taste) enriches the last 30 dishes end to end (one-line description, a 3-to-5-step `## Recipe`, `complexity`, and the optional cook fields where they apply). With this batch every dish in the library now carries a description and recipe: per-dish enrichment coverage reaches 100% of active dishes. Macros, tags, ingredient rows, catalog, engine.md, and the engine tests are untouched. This closes the B1 enrichment track (#38, #40, #41). (#41)

## 2026-06-12 Slice B1.2: enrichment batch 2 (31 dishes) + synthetic round-trip fixture

The second enrichment batch (`data/enrichment-2`, Rajat-reviewed for taste) enriches 31 more dishes end to end (one-line description, a 3-to-5-step `## Recipe`, `complexity`, and the optional cook fields where they apply), drawn from the dishes still lacking a `## Recipe`, with no overlap with prior batches. Per-dish enrichment coverage moves to 88 of 118 active dishes; macros, tags, ingredient rows, catalog, and `docs/engine.md` are untouched. Also converts the round-trip test's bare-dish fixture (`engine/test/data/dishFiles.roundtrip.test.ts`) from a real file (`lauki-sabzi`, now enriched) to a synthetic in-memory bare DishFile, so the test no longer depends on any real dish staying un-enriched; the "all live files round-trip byte-identical" case is unchanged. This permanently ends the per-batch fixture churn. (#40)

## 2026-06-12 Slice B3.1: library expansion batch 1 (20 new dishes for thin pools)

The first library-expansion batch (the §9-sanctioned `data/expansion-*` reviewed-content path, Rajat-reviewed), adding 20 new dishes (ids 140-159) chosen by the pool-coverage report to deepen the thinnest composition slots, each complete on arrival (full frontmatter, one-line description, a catalog-resolving `## Ingredients` table, a 3-to-5-step recipe; no photo, B2 track). Targets: Keto 7→11 (paneer tikka, prawn pepper fry, egg bhurji keto, tandoori soya chunks), Dessert (shrikhand, aamras, moong dal halwa), Rice variety (coconut/ghee/mint rice), Chapati 5→9 (bajra roti, missi roti), Chilla (moong dal chilla, oats chilla), Fruit 1→3 (banana bowl, papaya bowl), Bread (masala toast, bread upma), breakfast dry main (egg podimas), and a Menu 3 complete-meal HP option (prawn pulao). Three new catalog ingredients land with macros (Banana, Mango, Papaya, Other group). Every targeted thin pool deepened, no `<- thin` markers remain. The only non-content edit updates the `reports.test.ts` live-data assertion that hard-coded the old Fruit pool of 1 (non-blocking report snapshot, not a rule). The library grows from 121 to 141 dishes; the 5-week simulation stays valid. (#43)

## 2026-06-12 Slice 4.1: Convex schema extensions + nextWeekQueue + six mutations

Lands the additive Convex backend for the fast-loop day/dish editing the revamp introduces, all wired to write but not yet read (queries, generation-consumes-queue, and archive skip-exclusion are 4.2). `currentWeek` gains an optional `skippedDays` array (`{day, reason, author, skippedAt}`) and an optional per-dish `includeRecipe` flag (lives on the week so it resets each new week). `manualChanges.changeKind` gains `delete | add | skip_day | restore_day | save_next_week`, with `meal`, `position`, and `day` all loosened to optional (day-level and queue kinds have no meal/position; `save_next_week` has no day) — a required→optional loosening that keeps every existing row valid. A new `nextWeekQueue` table (`{createdAt, author, dishId, reason, status: queued|placed|dropped, consumedWeekStart}`) backs the save-for-next-week flow. Six mutations land in the new camelCase `app/convex/dayMutations.ts`: `deleteDish`, `addDish`, `skipDay`, `restoreDay`, and `saveForNextWeek` each write their `manualChanges` row in the same transaction (the `swapDish` pattern; reason required per Decision #8), while `setIncludeRecipe` writes none (it is a share preference, not a menu change). Every change is additive, so Convex validates all existing rows at deploy with no wipe-and-regenerate. `docs/engineering.md` §3/§5 rewritten to steady state. The current frontend keeps working untouched. (#42)

## 2026-06-12 Slice B1.1: enrichment batch 1 (30 more dishes)

The second reviewed-content enrichment batch on `data/enrichment-1` (the §9-sanctioned `data/enrichment-*` path, Rajat-reviewed for taste). Enriches 30 more dishes end to end (one-line description, a 3-to-5-step `## Recipe`, `complexity`, and the optional cook fields where they apply) drawn only from the dishes that lacked a `## Recipe`, spanning all categories and both meal-times with zero overlap with the slice-2.2 set. Per-dish enrichment coverage moves 30 to 60 of 118 active dishes (25 to 51%), the `npm run reports` burn-down meter; macros, tags, ingredient rows, and `docs/engine.md` are untouched. The only non-content edit repoints the round-trip test's bare-dish fixture from `aloo-beans` (now enriched) to `lauki-sabzi` (still bare); the per-file round-trip-byte-identical validator passes for all 121 files. (#38)

## 2026-06-12 Slice 3.2: requested dishes + explore ranking

Adds the last two engine rule areas of the 3.x set as new `docs/engine.md` sections (§6 Requested dishes, §7 Explore ranking; the spec-code parity section renumbers to §13 to stay last, all cross-references updated, the parity module map extended). Both ship as pure, fully-tested, **dormant** modules: 4.x wires requests into generation, 7.1 wires explore into the UI. **Requested dishes** (`engine/src/requests.ts`) gives `generateWeek` an optional additive `requests` input (a list of dish ids; default empty = behaviour-identical to today, so every current caller and the simulation stay green): each requested id is pinned, in order, to the first schedule slot whose §3 composition accepts it (Active, in-season, meal-time-matching, not already claimed), overriding §4 recency; an unplaceable request (out of season, inactive, unknown, or no free accepting slot) logs an incident and is not placed. A post-generation reconciliation re-checks each placement against the final week, guaranteeing the contract that a requested dish is **placed exactly once XOR produces an incident** (a dish dropped by the §9 cap, or whose pool a pick branch never drew from, is reported unplaceable and re-queues next week, matching the §1.8 slow-loop "composition keeps rejecting" signal). **Explore ranking** (`engine/src/explore.ts`) ranks eligible never-cooked dishes "familiar-but-new" by three signals normalized to [0,1] and summed: shared-primary-ingredient frequency with history, per-person protein-band proximity (via §11 nutrition) to the household cooked median, and category familiarity; ties break by id. Each ranked dish carries a structured `dominantAffinity` key (the argmax signal), never UI prose (Principle 7) — 7.1 phrases the "why it fits" line. A shared `candidateSetPools` helper is extracted into `composition.ts` and reused by `rankCandidatesForSlot` and the requests planner (deduplicating a copy that lived in `generateWeek`). Engine suite now 372 tests. (#39)

## 2026-06-12 Slice B2.1: dish-photo style spec

Commits `data/dish-photos/STYLE.md`, the single source of truth for how every Plantry dish photo looks, so the 121 current dishes and the ~80 expansion dishes generate as one consistent AI style (Decision #4). It fixes a verbatim prompt template (two slots, `{dish name}` and the one-line description, filled from the dish file; all surrounding wording frozen so the look cannot drift between runs) and the full style contract: directly-overhead flat lay, soft diffuse daylight, simple matte cream/terracotta stoneware, minimal props, plain warm-cream background, true-to-home muted color aligned to the handoff tokens, and a **square 1:1 crop** chosen because the PWA renders photos with `object-fit: cover` at two very different shapes (a ~40-48px rounded-square thumbnail and a full-width 96px Explore strip), so a centered square survives both without clipping. Output is a web-ready ~1024² JPEG under ~300KB with no post-processing dependency (the slice-1.2 no-image-library constraint), named `data/dish-photos/<slug>.jpg`; the `photo:` frontmatter is set later in the same batch as the real image. Scoped to the spec only: no images and no `photo:` fields this slice (image generation happens outside the session; photo batches are B2.2+), which keeps it parallel-safe with concurrent enrichment work. `docs/engineering.md` §14 now lists `data/dish-photos/` as a repo resident; the CI structure check and `MAINTENANCE.md` §2.9 inventory at root granularity and needed no change. (#37)

## 2026-06-12 Slice 2.2: catalog macros populated + first 30 dishes enriched

The first content batch on the design-revamp data layer (the `data/enrichment-*` reviewed-content path, Rajat-reviewed for taste). Populates `Protein /100g` and `Carbs /100g` for all 49 macro-relevant rows of the `data/ingredients.md` catalog with standard nutrition-table values, and `Grams per piece` for every `pcs`-unit ingredient (Egg 50, Bread 30, Pav Bread 40, Green Chilli 5, Lemon 60); aromatics, herbs, and the Other group stay blank by design (blank reads as zero). Macro coverage of the catalog goes 0 to 100%, so `nutrition.ts` now derives real per-person protein and carbs for every dish. Enriches a first 30 dishes end to end (one-line description, a 3-to-5-step `## Recipe`, `complexity`, and the optional cook fields where they apply) spanning all twelve categories and both meal-times; per-dish enrichment coverage goes 0 to 25% (30 of 118 active dishes), the coverage report's burn-down meter for later batches. Photos remain the separate B2 track (untouched). No engine, Convex, rule, or tag change; the only non-data edit is `engine/test/data/reports.test.ts`, whose two live-data assertions flipped from the pre-2.2 blank state to the post-2.2 reality (synthetic report-logic tests untouched). With macros populated the HP-vs-protein consistency report now speaks: 20 HP-tagged dishes derive under the 20 g-per-person threshold (an artifact of the per-person basis: modest protein quantities for two), surfaced as slow-loop signal with no tags changed. (#35)

## 2026-06-12 Slice 3.1: picker ranking + skipped days

Adds two engine rule areas as new `docs/engine.md` sections (the spec-code parity section renumbers to stay last, all cross-references updated, the parity module map extended). **Picker ranking** (`engine/src/pickerRanking.ts`) is a deterministic swap/add ranking over the broad, non-restrictive pool (Active + in-season + meal-time, per Principle 4), distinct from §4 generation priority: a head of "fits this day" dishes (meal-time match, not already on the day) ordered by a lexicographic tuple `(recencyTier, proteinBandDistance, id)`, then a tail of same-day repeats ordered the same way. `recencyTier` is a coarse longest-unused bucket (every never-cooked dish shares the best tier; cooked dishes tier by last-cooked weekStart) so the protein-band term genuinely orders within a tier without ever overriding recency; `proteinBandDistance` (swaps only) pulls candidates in the outgoing dish's per-person protein band (derived via `nutrition.ts`, 5 g buckets) toward the top; `id` is the final total tie-break. No RNG. `getSlotAlternatives` in `app/convex/swap.ts` switches from §4 `rankCandidates` to `rankPickerAlternatives` (ranking only; the broad-pool eligibility filter is untouched) so spec and shipped behavior match from day one. **Skipped days** makes grocery aggregation (`engine/src/groceryList.ts`) and a new pure finalize history-rows derivation (`engine/src/historyRows.ts`, `deriveHistoryRows`) skip-aware via an optional `skippedDays` input (default none, additive, every existing caller unchanged): a skipped day keeps its generated dishes in the data but contributes zero grocery rows and zero history rows (so recency never sees uncooked dishes). Generation itself is untouched; the Convex `skippedDays` schema (4.1) and the PWA "Skipped" rendering (5.x) wire these later. Adds a deterministic seeded 5-week forward simulation harness (`engine/test/simulation.test.ts`, previously a Stream B follow-up) with a skipped-day week and the property test that skipped days contribute zero grocery and zero history rows. Engine suite now 352 tests. `app/convex/swap.ts` changed (ranking call only); the Convex change is non-schema, so the deploy carries no row-validation risk. (#36)

## 2026-06-12 Slice 2.1: enrichment schema, derived macros, reporting layer

Adds the design-revamp derived-data layer with zero visible app change. The Dish schema, parser, and serializer gain optional enrichment frontmatter (`complexity` as an `Easy | Medium | Hard` enum, `skill`, `equipment`, `buySpecially`, `prePrep`, `photo`) plus body conventions: the first paragraph parses to a one-line `description` and a `## Recipe` section's numbered steps parse to `recipe`. All optional, so every current dish file (none enriched yet) round-trips byte-identical, with and without the new fields. `data/ingredients.md` gains three macro columns (`Grams per piece`, `Protein /100g`, `Carbs /100g`), schema-and-columns only: every cell ships blank (population is slice 2.2), and a blank reads as zero. New `engine/src/nutrition.ts` derives per-dish macros, never hand-stored and with no override field (Principle 8): protein and carbs per person are `Σ over ingredient rows of (grams × per-100g ÷ 100) ÷ 2` (the ÷2 is the two-person household basis), `pcs` rows convert via `Grams per piece` and `ml` rows convert to grams 1:1 (culinary liquid density ≈ 1, so milk and coconut milk derive correctly), plus a protein-to-carb ratio that is null when carbs are zero. A non-blocking reporting layer joins the blocking validators in `engine/src/data/validators.ts` (coverage report over enrichment fields and macro-relevant catalog rows; pool-coverage per composition slot per season; HP-vs-protein consistency at a reporting-only 20 g-per-person threshold), surfaced by a new `npm run reports` (`engine/scripts/reports.ts`); near-zero macro coverage pre-2.2 is expected, not a failure. `docs/engine.md` gains a §7 Nutrition section (with the reporting layer) and an extended field reference, with the spec-code parity section renumbered to §9 so it stays last. Engine suite now 328 tests. (#34)

## 2026-06-11 Slice 1.2: per-dish files + ingredient catalog migration

Replaces the two monolithic data files with the design-revamp canonical layout, a pure refactor with zero behavior change proven by a golden-master test (`generateWeek` byte-identical across a 576-case input matrix before and after; the golden master and the one-shot migration script were both deleted in the same PR per plan §8). `data/dishes.md` explodes into 121 per-dish `data/dishes/<slug>.md` files (YAML frontmatter for the existing structured fields plus a `## Ingredients` table; no enrichment fields yet, those are slice 2.x); collision slugs are disambiguated by id (the two "Paneer bhurji" dishes become `paneer-bhurji` for id 13 and `paneer-bhurji-106` for id 106). `data/ingredients.md` becomes a 59-row ingredient catalog (`Ingredient | Group | Unit | Pack Size`, 8 tracked) and is now the single source of truth for grocery grouping: the hand-maintained `GROCERY_GROUPS` map retires from `engine/src/groceryList.ts`, its values and documented judgment calls carried over verbatim. Blocking validators land in `engine/src/data/validators.ts`: every dish ingredient row resolves to a catalog row by exact name (the anti-spelling-drift gate that protects future ordering automation), every catalog row has a Group, frontmatter validates against the schema, ids and slugs are unique and filenames match, and per-file round-trip is byte-identical. Parsers, serializers, schemas, the build-time bake (`engine/scripts/bake.ts`, now runs validators), and the Convex grocery query move to the new layout; `yaml` is added as an engine dependency. The name-resolution validator covers ingredient rows only; `primaryIngredient` stays a free categorization label (tracked follow-up). Seven dangling ingredient rows for a non-existent dish id 7 ("Rajma"; the real dish is id 99 "Rajma chawal") were dropped as dead data with no behavior impact. Lockstep path updates across `docs/engine.md`, `docs/engineering.md`, `MAINTENANCE.md`, `.claude/commands/slow-loop.md`, and `docs/development.md`. Deploy Convex verified green post-merge (the prod bake reads the new per-dish layout). (#32)

## 2026-06-11 Slice 1.1: design-revamp plan committed; root inventories aligned

Opens the design-revamp feature (the June 2026 Claude Design handoff). Commits the planning artifacts that until now lived only as uncommitted files in the main coordination directory: `features/design-revamp.md` (the ten-slice execution plan), `claude-design.md`, the whole `design_handoff/` folder (handoff README, hi-fi screens, primitives, tokens, and 33 dish reference photos), the CLAUDE.md "Currently building" line, and the DECISIONS.md planning entries. Aligns the three drifted root-inventory surfaces onto one canonical membership list: the `.github/workflows/ci.yml` structure-check regex (now admits `claude-design.md` and `design_handoff`, committed in the same change so the check does not reject its own residents), `MAINTENANCE.md` §2.9 (full files-and-dirs inventory, previously missing `.githooks`, `.maintenance-state`, `.prettier*`, `scripts`, and the root config files), and `docs/engineering.md` §14 (prose tree plus a root-markdown naming exception for lowercase `claude-design.md`). `docs/development.md` §9 now sanctions reviewed content-batch PRs (`data/enrichment-*`, `data/photos-*`, `data/expansion-*`) as a second legitimate canonical-data path alongside the slow loop, and §2 gains those branch conventions. ESLint and Prettier ignore `design_handoff/` (vendored design-reference artifacts, not project source). No engine, Convex, or app code touched; no behavior change. (#31)

## 2026-06-10 Stream I: manual-changes log + required reason

New append-only `manualChanges` table in `app/convex/schema.ts` captures every swap and custom one-off the user makes to the live week, with `before`, `after`, freeform `reason`, `author`, `weekStart`, `day`, `meal`, `position`, `changeKind`, and the standard slow-loop status lifecycle (`queued` / `in_review` / `applied` / `dismissed` / `reviewed_no_change`). `swapDish` and `addCustomOneOff` now require a non-empty `reason: string`, trim it, and insert the log row in the same Convex transaction. `app/web/src/components/SlotEditor.tsx` gains a shared `ReasonField` used by both SwapPane and CustomPane: 5 quick-fill chips (`Bored of it`, `Not in mood`, `Missing ingredient`, `Tuhina wants this`, `Want a change`) above a required text input; Swap/Save is disabled until reason has non-whitespace text after trim. The slow-loop skill at `.claude/commands/slow-loop.md` is extended to read `queries/manualChanges:listQueuedManualChanges` alongside queued comments and incidents, cluster across all three signals, and emit a `manual_change_ids:` line in each per-cluster fence; the slow-loop-applied GitHub Action and `scripts/slow-loop-mark-applied.mjs` parse the new field and call `manualChangesMutations:markManualChangesApplied` or `markManualChangesReviewedNoChange` per cluster outcome, with backward-compat flat-list fallback. Crucially the skill states "the slow loop does NOT treat a swap as a violation; it reads the swap's before, after, reason, day-meal-position context as signal for what the engine got wrong, then asks whether the rule should change", closing the framing gap Stream H §6a had left open. Pure additive Convex schema change (no existing tables touched), so Deploy Convex passes without the wipe-and-regenerate path. Fixture at `data/test-fixtures/slow-loop/manual-changes.example.json`. (#27)

## 2026-06-10 Stream H: multi-dish slots (post-v1 structural fix)

v1 shipped with `app/convex/schema.ts` modelling one `dishId` per `(day, meal)` and `app/convex/generateWeek.ts` persisting only `slot.dishes[0]`; the engine generated the correct counts per `docs/engine.md` §2-3 but the schema flattened N picks to 1, so the dashboard rendered one item per meal and the grocery list silently under-counted. Stream H reshapes `currentWeek.slots[]` to shape (a): one row per (day, meal) carrying a `dishes: Array<{ dishId, customLabel, source, author, updatedAt }>`. The frontend (`CurrentWeekView.tsx`) now renders one row per position with the meal label on row 0 only; each row gets its own Edit and Comment buttons. The swap picker is non-restrictive per `docs/product.md` §4 Principle 4: `getSlotAlternatives` and `swapDish` take `position: number` and `app/convex/swap.ts:broadPool` filters only by Active + season + meal-time, no per-position (HP/partner/Option A-B-C/carb) eligibility. `swapDish` drops the prior `dish-not-eligible` re-rank; hard rejections retained are `dish-not-meal-time`, `dish-not-active-or-in-season`, `dish-not-in-library`. The §6a learning signal (incident-on-rule-violating-swap) is deferred to a tracked Stream H.1 follow-up in `features/multi-dish-slots.md`. Net effect on the live week after `generateCurrentWeek`: Mon/Wed/Fri 5 rows (2 breakfast + 3 lunch), Tue/Thu 5 rows (1 breakfast + 4 lunch), Sat 3 rows. (#26)

## 2026-06-09 Plantry v1 ships

Phase 2 closes. Both phones have a working PWA at `plantry.mudgal.xyz` (passcode-gated, identity-aware) that renders an engine-generated Monday-to-Saturday menu, supports inline dish swaps with engine-ranked alternatives, custom one-off entries, comments on dishes and days, and a grouped grocery list below the week. Stream A (data layer: parsers, validators, library bake) shipped in three slices (PRs #3, #7, #10). Stream B (engine) shipped in eight slices covering eligibility (§1), schedule (§2), composition (§3 including §3.1 lunch carb and §3.2 weekday substitution), priority (§4), consolidation (§6), cap (§5), `generateWeek` and `rankCandidatesForSlot` composing the pipeline, and the grocery aggregator (PRs #5, #8, #12, #14, #16, #18, #19, #23); only the 5-week simulation harness remains as a follow-up. Stream C (Convex backend) shipped schema audit + read-only queries + dev seed, three browser-callable mutations (`addComment`, `setUserProfile`, `addCustomOneOff`), `generateCurrentWeek` (engine-to-Convex bridge), `swapDish` + `getSlotAlternatives`, and `getGroceryList` (PRs #6, #15, #20, #21, #24); `finalizeWeek`, auto-recovery middleware, and the Swiggy MCP integration are queued for follow-ups. Stream D (frontend) shipped read-only week view, three write affordances (identity Convex sync, custom one-off, comments composer + list), swap UI, and grocery list rendering (PRs #13, #17, #22, #24); menu image (PNG) generation is the next queued follow-up. Stream E (slow-loop) shipped the slash command + fixtures, the consumed-rows GitHub Action, and the first end-to-end slow-loop PR (PRs #4, #9, #25). Stream F (identity + concurrency hardening + per-PR Convex preview branches) is post-v1. Open follow-ups, in rough priority order: Tuhina onboards her phone; the simulation harness (B slice 9); `finalizeWeek` and auto-recovery middleware in Convex; menu image (PNG) generation; Stream F hardening; and the day-level overrides Phase per `docs/product.md` §8. `features/phase2.md` moves to `archive/features/phase2.md`; `CLAUDE.md`'s "Currently building" line returns to `_none_`.

## 2026-06-09 Milestone 2.4: first slow-loop PR end to end

The EM runs the first slow-loop pass against the synthetic fixtures in `data/test-fixtures/slow-loop/`. Five clusters considered (one positive, four diagnostic); every cluster resolves to `reviewed_no_change` per the right-size discipline (one comment or two cases within a single week does not justify a structural change). The PR's structural effect is one entry appended to `data/changelog.md` plus `.maintenance-state.last_slow_loop` advancing from `never` to `2026-06-09`. The validated pipeline: slow-loop slash command (PR #4) -> EM-authored diagnosis cards with the per-cluster fenced section (PR #9 amendment) -> PR opened on a `slow-loop/<date>` branch -> CI passes -> squash-merge -> `slow-loop-applied.yml` GitHub Action triggers and exits clean (fixture IDs are not real Convex IDs, so each `markCommentsReviewedNoChange` call skips them per the action's never-throw design and writes a warning incident on each). The Phase 2 ship-checklist box "At least one slow-loop PR has been merged end to end" goes green. (#25)

## 2026-06-09 Stream C slice 5 + Stream D slice 4: grocery list query and rendering

`app/convex/groceryList.ts` ships `getGroceryList({ weekStart }): GroceryList`, a browser-callable query that walks `currentWeek.slots[]`, looks up each `dishId` in the baked library, skips custom one-offs (whose ingredients are not modelled in v1), and hands off to `aggregateGroceryList` for the actual aggregation + grouping + pack rounding. Throws `ConvexError("no current week for this weekStart")` when the row is missing; callers are not supposed to ask before `getCurrentWeek` returns non-null. The frontend gains `app/web/src/components/GroceryList.tsx` rendered below the week body in `CurrentWeekView`; it subscribes to `getGroceryList`, renders one section per group in the fixed §3 order with each item showing ingredient name + the rounded total for tracked items ("400 g (2 packs)") or raw sum for untracked ("150 g", "2 pcs"). 70 lines of CSS for the grouped list. The engineer initially shipped a local-stub copy of the aggregator (option 2 from the brief) because Stream B slice 8 had not pushed yet; the EM rebased onto post-slice-8 main, deleted the 230-line stub block, and swapped in the `@plantry/engine` import without changing the query handler. (#24)

## 2026-06-09 Stream B slice 8: grocery list aggregator

`engine/src/groceryList.ts` exports `aggregateGroceryList({ weekPicks, ingredients, packSizes }): GroceryList` plus the `GroceryItem` and `GroceryGroup` types and a `GROCERY_GROUPS: Record<string, GroceryGroup>` constant covering the 59 canonical ingredient names from `data/ingredients.md`. The aggregator walks each picked dish's ingredient rows, sums quantities by `(ingredient, unit)` so the same item in two units stays separate, applies pack-size rounding via `Math.ceil(quantity / packSizeGrams)` for ingredients in the pack-size header (storing both raw `quantity` and rounded `packTotalGrams`), categorizes via the constant with "Other" as fallback, sorts items alphabetically within each group, emits groups in the fixed §3 order (Proteins and Dairy, Pantry, Vegetables, Aromatics and Herbs, Other), and skips empty groups so the frontend does not render a stray header. Judgment calls on grouping are documented inline (Capsicum/Cucumber as Vegetables; Onion/Tomato/Lemon as Aromatics; Coconut Milk and Sprout as Pantry; Fruit as Other). Unit guard on tracked ml/pcs ingredients matches the consolidation slice's pattern; today's live data never trips it. 14 unit tests covering single-dish, multi-dish aggregation, pack-boundary rounding, untracked passthrough, empty input, unknown-category fallback, sort + omit-empty, and a permutation property. Engine suite now 168 tests across 15 files. Stream B has only the 5-week simulation harness left. (#23)

## 2026-06-09 Milestone 2.2: dish swap

Two parallel PRs land together. (#21) `app/convex/swap.ts` exports `getSlotAlternatives({ weekStart, day, meal, limit? }): Dish[]` (engine-ranked alternatives for a slot via `rankCandidatesForSlot`, current-dish filtered out, top 10 default) and `swapDish({ author, weekStart, day, meal, newDishId, version }): { ok: true; version } | { ok: false; reason: ... }` (validates author, version, slot, library membership, then re-runs `rankCandidatesForSlot` to catch the stale-tap race; patches with `source: "swapped"`, increments version). `seasonOf` is duplicated inline from `generateWeek.ts` (two callers below the abstraction bar per `docs/product.md` §4 Principle 8). (#22) `SlotEditor` refactored into shared chrome plus two mode panes: a Swap pane subscribes to `getSlotAlternatives` and renders the alternatives list with an HP chip and single-select interaction; a Custom pane is the slice-2 free-text editor unchanged. Segmented control switches modes. Result handling covers the five reasons: `version-mismatch` and `dish-not-eligible` get targeted inline messages with a Reload, the three bug-shaped reasons (`no-current-week`, `no-such-slot`, `dish-not-in-library`) get a single "Something is off, please reload." 44 px tap targets. (#21 + #22)

## 2026-06-09 Stream C slice 3: generateCurrentWeek bridges engine to Convex

`app/convex/generateWeek.ts` ships `generateCurrentWeek`, an `internalMutation` that calls the engine's `generateWeek` and persists the result into the `currentWeek` table. Inputs: `weekStart` (ISO Monday), optional `rng` (a numeric value wrapped as a constant-returning closure for deterministic Saturday alternation in tests), and optional `userRequestedDishId` (for §3.2 substitution). The function reads the baked library from `@plantry/engine/library` and the seed history from `@plantry/engine/history`, derives the Bangalore season from the ISO date string directly (no Date object), calls the engine, then converts: engine days are already short-form so the day map is identity; meal lowercase ("Breakfast" -> "breakfast"); per slot it emits `{ day, meal, dishId: slot.dishes[0].id, customLabel: null, source: "generated", author: "system", updatedAt }`. Slots whose `dishes[]` ended up empty (cap dropped them) are omitted. Replacement semantics: if a row exists for the same `weekStart`, it is deleted before insert; new row starts at `version: 1`, `status: "draft"`. Engine warnings (e.g. "Friday over cap (5), dropped: Bread omelette") become one `incidents` row each with `source: "engine"`, `severity: "warn"`, `context: { weekStart, weekId }`. Return shape: `{ weekId, version, incidentCount }`. Not browser-callable; the EM (or a future Stream F scheduler) invokes via `npx convex run generateWeek:generateCurrentWeek '{"weekStart":"YYYY-MM-DD"}'`. (#20)

## 2026-06-09 Stream B slice 7: generateWeek and rankCandidatesForSlot

The two public engine APIs that compose §1 through §6 land. `generateWeek({ weekStart, library, history, season, ingredients, packSizes, rng?, lastSaturdayMenu?, userRequestedDishId? }): GeneratedWeek` walks the schedule, composes a candidate set per slot, ranks within each candidate pool with the consolidation ledger threaded through, picks the top of each pool, updates the ledger via `applyPick`, applies §3.2 weekday substitution before iterating when `shouldSubstituteWeekday` fires, projects the day's picks into a `Map<Day, Dish[]>` and hands off to `applyCap`. Cap drops are reported in `GeneratedWeek.droppedDishIds` plus a human-readable line in `incidents` (e.g. "Friday over cap (5), dropped: Bread omelette"). Menu 1's partner selection branches on the HP pick's category (Dry → non-HP Gravy; Gravy → Accompaniment). `rankCandidatesForSlot({ ... currentWeekPicks })` runs the same pipeline for a single slot, used by the swap UI's auto-suggest. 14 new tests including a deterministic-RNG check, §3.1 Rice-at-most-once across the week, §3.2 user-requested substitution, and live-data smoke across all three Bangalore seasons. Engine suite now 154 tests across 14 files. Stream B engine pipeline is complete; only the 5-week simulation harness remains. (#19)

## 2026-06-09 Stream B slice 6: item cap per docs/engine.md §5

`engine/src/cap.ts` exports `applyCap({ slotsByDay }): { slotsByDay; droppedDishIds[] }` plus `WEEKDAY_CAP = 5` and `SATURDAY_CAP = 3`. When a day exceeds its cap, dishes are dropped one at a time: lowest Satiety first; among those the longest Prep Min; final tie-break (silent in §5 itself) drops the later array position so earlier slots' picks win. The function is pure and returns a fresh `Map<Day, Dish[]>` plus the dropped ids. Sunday is passed through unchanged (defensive, since §2 schedule emits no Sunday slots). `SlotPick` is exposed as a `Dish` alias so the in-flight `generateWeek` slice can widen it to carry per-slot metadata without rewriting the cap algorithm. 14 unit tests cover under-cap passthrough, at-cap passthrough, weekday + Saturday over-cap, tie-break ladder, multi-day independence, and the "set difference" permutation property. Engine suite now 140 tests across 12 files. (#18)

## 2026-06-09 Stream D slice 2: identity Convex sync, custom one-off editor, comments composer

PWA now writes. On identity pick, `setUserProfile({ deviceId, identity })` runs alongside the localStorage write; `deviceId` is generated once via `crypto.randomUUID()` (or a fallback) and persisted under `plantry:deviceId`. The current-week view gains an inline `SlotEditor` per slot that calls `addCustomOneOff` with the live `currentWeek.version` and handles the tagged-union result: `{ ok: true }` collapses the editor and lets the Convex subscription re-render; `{ ok: false, reason: "version-mismatch" }` shows "Someone else just updated this week" with a Reload button; the other reasons show "Something is off, please reload." A `CommentComposer` opens below each slot (kind `dish`) and below each day header (kind `day`), posts to `addComment` with optimistic local append, and the new `CommentsList` shows queued comments for the visible week using `listQueuedComments`. 320 lines of mobile-first CSS added; 44 px tap targets documented inline in a CSS comment. Also folds in a small chore: the C-slice-2 mutation files `comments-mutations.ts` and `week-mutations.ts` are renamed to `commentsMutations.ts` and `weekMutations.ts` because Convex rejects hyphenated module names; the Deploy Convex action failed silently on the PR #15 and PR #16 merges to prod and is now green again on this commit. (#17)

## 2026-06-09 Stream B slice 5: ingredient consolidation per docs/engine.md §6

`engine/src/consolidation.ts` models the weekly ingredient buy-list state and provides preference scoring against it. `IngredientLedger` is a `Map` keyed by canonical ingredient name from the pack-size header; entries carry `packSizeGrams`, `packsOnBuyList`, `usedGrams`, `leftoverGrams`. `emptyLedger(packSizes)` initialises one tracked entry per pack-size row; untracked ingredients (everything not in the header: fresh produce, pantry staples) never enter the ledger per §6 wording. `applyPick(ledger, dish, ingredients)` walks the dish's ingredient rows in grams, adds enough packs to cover usage (rounded up to the next pack multiple), and recomputes leftover. `scoreCandidates(pool, ledger, ingredients)` orders candidates by how many ledger entries above the 50 g threshold they would consume; ties preserve input order. `scoreSoftConsolidation(pool, lastFreshItemsUsed)` adds the §6 soft preference on capsicum, tomato, cucumber, onion, mint, coriander; canonical mint/coriander names in `data/ingredients.md` are "Mint Leaf" and "Coriander Leaf" and both forms are in the constant. `rankByConsolidation` composes hard score primary, soft score secondary. The §4 step 3 placeholder shipped in B slice 4 is now wired to `byIngredientConsolidation` when a ledger is supplied; absent ledger preserves slice-4 no-op behaviour. Unit handling assumption documented inline: only `unit === "g"` ingredient rows are consolidated against gram pack sizes; ml / pcs rows fall back to no consolidation (no tracked ingredient is ml or pcs today; the guard is defensive). 23 new consolidation tests plus 4 priority extension tests; engine suite now 126 tests across 11 files. (#16)

## 2026-06-09 Stream C slice 2: addComment, setUserProfile, addCustomOneOff mutations

Three browser-callable Convex mutations land. `addComment({ author, attachedTo, text }): Id<"comments">` inserts a queued comment after trimming text and rejecting empty strings; the slow loop is the only consumer downstream. `setUserProfile({ deviceId, identity }): Id<"userProfiles">` upserts via the `by_deviceId` index, preserving `installedAt` on patch (the identity-setting mutation is itself the author-identification step, so it does not take an `author` argument). `addCustomOneOff({ author, weekStart, day, meal, customLabel, version }): { ok: true; version } | { ok: false; reason: "version-mismatch" | "no-current-week" | "no-such-slot" }` is a tagged-union return so the UI can branch on expected control flow (version mismatch is reload-and-retry, not an exception). Optimistic concurrency on `currentWeek` works by reading the row through the `by_weekStart` index, comparing the caller's `version`, patching the slot, and incrementing `version`. Shared `assertAuthor` helper in `app/convex/lib/author.ts` is reused across the mutations and is ready for `swapDish` and `finalizeWeek` when they land. (#15)

## 2026-06-09 Stream B slice 4: selection priority per docs/engine.md §4

`engine/src/priority.ts` exports `rankCandidates({ pool, history, sameDayBreakfastPrimaryIngredient?, consolidationContext? }): Dish[]` plus four per-step helpers. Step 1 (longest unused) does a stable index-decorated sort with the §4 recency exemption: dishes tagged `fruit` and lunch carbs (Category in {Chapati, Rice}) collapse to neutral so step 1 cannot reorder them relative to anything. Step 2 (same-day Primary Ingredient deprioritisation) partitions the pool into kept-above and pushed-below while preserving prior-step order in each group; when every candidate would match the breakfast primary ingredient, the original pool is returned unchanged (§4 "if no viable alternative remains, allow the repeat" fallback). Step 3 (ingredient consolidation) is a no-op placeholder; the real implementation lands with B slice 5 (§6 ingredient consolidation). Step 4 (Preferred=Yes over No) is a stable partition. `rankCandidates` composes the four steps in order so each step breaks ties from the previous. 18 unit tests cover per-step, exemption, tie-break, end-to-end composition, and a permutation property; engine suite now 99 tests across 10 files. (#14)

## 2026-06-09 Stream D slice 1: app shell, passcode gate, identity picker, read-only week view

The PWA now serves a usable shell. Splash page reads `VITE_PLANTRY_PASSCODE` and gates access; on success it persists a one-week `plantry:auth` token in localStorage. Identity picker writes `plantry:identity` in localStorage only (Convex `userProfiles` sync deferred to a later slice via a one-line code comment; the "Not me" link in the header clears it). The current-week view subscribes to `getCurrentWeek` via `useQuery(anyApi.queries.week.getCurrentWeek, {})`, handles three states (loading with offline cached-week fallback, no-current-week, has-current-week), and translates Convex's short-form day codes (`Mon` through `Sat`) into full-word labels at the view layer. Slot dish names come from the baked `library.ts`, exposed via a new `@plantry/engine/library` subpath export and looked up against a `dishId → name` map. The Workbox service worker caches the app shell; the last successful `getCurrentWeek` response is mirrored to `localStorage:plantry:lastWeek` so offline loads render the last known menu with a banner. `@plantry/web` gained a `prebuild` script that runs the engine bake so Vercel finds `library.ts` during deploy. (#13)

## 2026-06-08 Stream B slice 3: composition per docs/engine.md §3

`engine/src/composition.ts` implements every §3 sub-clause as a named helper and a top-level `composeSlot(args): CandidateSet` entry point. `CandidateSet` is a discriminated union keyed by `kind`, one variant per menu form (`breakfast-pair`, `breakfast-single`, `menu-1` through `menu-4`); each variant exposes the per-position dish pools rather than picking among them — selection between pools is §4 priority's job. Breakfast Mon/Wed/Fri returns all three Options (A/B/C) as parallel pools; Menu 1 exposes both partner pools (Dry-paired and Gravy-paired) since the HP pick decides which. §3.1 lunch carb rule defaults to Chapati, allows Rice exactly once per week via a `weekLunchCarbs` arg, and skips the recency filter per spec. §3.2 weekday substitution detector takes an optional `userRequestedDishId` for the future swap-UI hookup and otherwise fires when the longest-unused complete_meal Lunch dish is older than the protein candidate (HP for Menu 1, Keto for Menu 2). 32 unit tests cover every sub-clause with hand-built fixtures; engine suite now 81 tests across 9 files. (#12)

## 2026-06-08 Reconcile open items 12, 13, 14

Three reconciliation items batched into one chore PR after Rajat's decisions. **Item 12 (`weekArchive` shape):** `app/convex/schema.ts` `weekArchive.rows[].day` is now a union of `Monday` through `Sunday`, `meal` is `Breakfast` or `Lunch`. `docs/engineering.md` §3 spelled out to match. `currentWeek.slots[]` left in its short/lowercase form because it represents the live in-progress plan rather than a snapshot. Convex codegen flows through `_generated/dataModel.d.ts` without hand-edits. Stream D is now unblocked. **Item 13 (Rajma):** Rajma was the dish referenced by the 2026-05-04 Thursday lunch row in `data/menu_history.md` but absent from `data/dishes.md`; per Rajat's call ("don't need Rajma by itself"), the row is removed from history and the `KNOWN_MISSING_DISH_IDS` exception in `engine/scripts/bake.ts` is dropped. Bake count goes from 120 to 119 history rows. Tests pruned accordingly; engine suite is 49 tests across 8 files, all green. **Item 14 (em-dashes):** Project style rule clarified in `CLAUDE.md`: em-dashes are banned in user-facing content (PWA UI, menu images, grocery lists, share images); internal docs may use them, sparingly. `MAINTENANCE.md` left untouched. (#11)

## 2026-06-08 Stream A slice 3: bake typed library and history from data/\*.md

New build step that closes Stream A. `engine/scripts/bake.ts` reads `data/dishes.md`, `data/ingredients.md`, `data/menu_history.md`, runs the existing parsers and cross-file validators, and emits typed runtime modules `engine/src/data/library.ts` (exports `dishes`, `packSizes`, `ingredients`, plus a `library` bundle) and `engine/src/data/history.ts` (exports `history`). Both modules carry a generated-by header and stay gitignored. Wired into npm scripts (`npm run bake` from root, `prebuild` in the engine workspace) and into CI as a step before typecheck, so any data or schema drift fails CI loudly. Encodes one temporary exception via `KNOWN_MISSING_DISH_IDS = new Set([7])` for the open Rajma drift (open item 13); removable in one line once the dish lands in `data/dishes.md`. 4 new bake tests; engine suite now 33 tests across 7 files. Stream A's three-slice arc is complete. (#10)

## 2026-06-08 Stream E slice 2: mutations and GH Action to mark consumed slow-loop rows

Three `internalMutation`s in `app/convex/comments.ts`: `markCommentsApplied`, `markCommentsReviewedNoChange`, `markIncidentsResolved`. Each is best-effort, never throws, and writes a `warn`-severity `incidents` row when a target id is missing or already resolved. New GitHub workflow `.github/workflows/slow-loop-applied.yml` triggers on `pull_request` closed when `merged === true` and `head_ref` starts with `slow-loop/`, then runs `scripts/slow-loop-mark-applied.mjs` against the merged PR body, parsing a `## Consumed comments by cluster` section with per-cluster `outcome`, `comment_ids`, `incident_ids`, falling back to flat consumed-IDs lines when the per-cluster section is absent. Calls the three mutations via `npx convex run --prod` using the existing `CONVEX_DEPLOY_KEY` secret. Closes the slow-loop feedback cycle. One-sentence edit to `.claude/commands/slow-loop.md` requires the per-cluster section in real slow-loop PRs. New §3 in `MAINTENANCE.md` documents the action's contract. (#9)

## 2026-06-08 Stream B slice 2: weekly schedule per docs/engine.md §2

`engine/src/schedule.ts` exports `weekSchedule({ weekStart, lastSaturdayMenu?, rng? }): SlotPlan[]`. SlotPlan carries `day`, `meal`, `itemCount`, and `lunchMenu` (1 to 4, lunch only). Mon/Wed/Fri are 5-item days (Menu 1, 3-item lunch); Tue/Thu are 5-item days (Menu 2, 4-item lunch); Saturday has no breakfast and lunch is 3 items of Menu 3 or 4 alternating with the previous Saturday (random fallback when history is empty). Sunday emits no slots. Pure: no I/O, no library coupling. The caller resolves `lastSaturdayMenu` from history and library tags so schedule.ts stays library-free. ISO Monday calendar validation on `weekStart`. 18 unit tests (47 total engine tests now). §3.2 weekday substitution is deferred to composition. (#8)

## 2026-06-08 Stream C slice 1: schema audit, read-only queries, dev seed

Three new Convex queries under `app/convex/queries/`: `getCurrentWeek` (latest `currentWeek` row or null), `listQueuedComments` (queued comments ascending by `createdAt`), `listIncidents` (open incidents descending by `createdAt`). Plus `app/convex/seed.ts` exporting `seedCurrentWeek`, an internal mutation that inserts a sample week for dev, runnable as `npx convex run seed:seedCurrentWeek`. Schema audit against `docs/engineering.md` §3: matches spec on all five tables. Indexes present in the schema (used by these queries) are not enumerated in §3; deferred to a docs maintenance pass. `app/convex/_generated/` is now tracked in git so CI's typecheck has the types without needing a Convex deploy key on every PR. (#6)

Mutations (`swapDish`, `addCustomOneOff`, `addComment`, `finalizeWeek`), auto-recovery middleware, and `getGroceryList` are deferred to later Stream C slices that depend on Stream B engine integration.

Surfaces the same `weekArchive` vs `MenuHistoryRow` shape question PR #7 raised; reconciliation still deferred.

## 2026-06-08 Stream B slice 1: engine eligibility

`engine/src/eligibility.ts` exports `eligibleDishes({ library, history, season, slot })` mirroring `docs/engine.md` §1. The §1 predicates implemented are `Active=Yes` and the seasons match (`seasons === "All" || seasons.includes(season)`). Pure functions, no I/O, library order preserved. The `slot` argument is accepted for forward-compatible signature but not consumed by §1 itself (Time-vs-meal match is §3 composition's job). 14 unit tests cover each predicate independently and combined; total engine suite now 29 tests across 6 files. (#5)

Foundation for `schedule.ts`, `composition.ts`, `priority.ts`, `cap.ts`, `consolidation.ts`, and the `generateWeek` and `rankCandidatesForSlot` public APIs, all queued for later Stream B slices.

## 2026-06-08 Stream A slice 2: menu_history parser + cross-file validators

`MenuHistoryRow` Zod schema in `engine/src/data/schemas.ts` (plus reusable `DayNameSchema` and `IsoDateSchema`). `parseMenuHistory` walks the multi-section `## Week of <date>` structure in `data/menu_history.md` and validates rows with row-named errors. `serializeMenuHistory` round-trips byte-identical. Two cross-file validators in a new `engine/src/data/validators.ts`: `validateMenuHistoryAgainstLibrary` (every dish id in history exists in dishes) and `validatePackSizesUsed` (every tracked ingredient in the pack-size header is used somewhere). Both throw a single message listing every offender. (#7)

Surfaces two follow-ups: (1) `weekArchive` shape in `app/convex/schema.ts` does not currently match `MenuHistoryRow` casing or `weekStart` placement; spec says they should mirror; reconciliation deferred. (2) Live `data/menu_history.md` references dish id 7 (Rajma) that is absent from `data/dishes.md`; validator caught it; route to slow-loop.

## 2026-06-08 Stream E slice 1: slow-loop slash command + dry-run fixtures

Defines `/slow-loop` as a Claude Code slash command at `.claude/commands/slow-loop.md`. The prompt inlines the right-size discipline (`docs/product.md` §4 Principle 1) and the diagnosis card format (`docs/development.md` §5), supports a `--fixture <path>` argument for EM dry-runs before real comments accumulate, and names the production Convex deployment (`disciplined-chameleon-263`) explicitly. Adds synthetic queued-comments and incidents JSON fixtures at `data/test-fixtures/slow-loop/` matching the `comments` and `incidents` table shapes in `app/convex/schema.ts`, including a cross-input cluster (paneer fatigue across two comments plus a recency incident) so the EM can exercise clustering judgement during dry-run. (#4)

## 2026-06-08 Stream A slice 1: dish + ingredient parsers

Typed schemas and round-trip parsers for `data/dishes.md` and `data/ingredients.md`. Adds `zod` to the engine package and a small set of Zod schemas + inferred TS types for `Dish`, `Ingredient`, `PackSizeHeader`. `parseDishes` and `parseIngredients` validate via Zod and throw row-named errors on bad input. `serializeDishes` and `serializeIngredients` round-trip byte-identical to the source files (modulo a documented whitespace rule). Round-trip tests run against the live data files in CI. Out of scope and queued for later Stream A slices: `data/menu_history.md` parser, cross-file validators, the build-pipeline emit of `library.ts` / `history.ts`. (#3)

## 2026-06-08 Deploy pipeline live; custom domains resolving

DNS auto-configured via the Vercel-Cloudflare integration; `plantry.mudgal.xyz` and `plantry-dev.mudgal.xyz` both serve the PWA over HTTPS (200 OK). The GitHub-Vercel integration auto-deploys the frontend on push to main. A new `Deploy Convex` GitHub Action deploys schema and functions to `disciplined-chameleon-263.convex.cloud` on push to main when `app/convex/`, `engine/`, or `data/` changes. CI workflow runs unchanged on every PR and push.

## 2026-06-08 Bootstrap: monorepo, PWA shell, Convex schema, hooks, CI

Stream 0 ships. npm workspaces across `engine/`, `app/web/`, `app/convex/` with shared TypeScript config (strict). The frontend is a minimal Vite + React + Workbox PWA loading a Hello Plantry page. `app/convex/schema.ts` declares the five runtime tables (`currentWeek`, `weekArchive`, `comments`, `incidents`, `userProfiles`) from `docs/engineering.md` §3. A pre-commit hook installed by `scripts/install-hooks.sh` refuses code-path commits from the main coordination directory while allowing them from worktrees, detecting which by whether `.git` is a directory or a file at the toplevel. CI runs lint, typecheck, frontend build, and the engine smoke test on Node 20. ESLint 9 flat config and Prettier with sensible defaults. (#1)
