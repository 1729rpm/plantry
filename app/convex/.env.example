# Shared passcode that gates the public URL.
# Pick something Rajat and Tuhina both know; rotate by editing in the Convex dashboard.
APP_PASSCODE=

# Token used by the slow-loop session to read queued comments via npx convex run.
# Generate a random string (any length) and set the same value here and as a
# GitHub Action secret if the slow loop ever runs in CI.
SLOW_LOOP_TOKEN=
