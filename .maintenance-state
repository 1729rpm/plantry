| pass    | last-run   | status  | deferred | note                                    |
| ------- | ---------- | ------- | -------- | --------------------------------------- |
| signals | 2026-09-07 | done    | 2        | dislikes listing query owed as a chore  |
| health  | 2026-09-07 | done    | 6        | exploration pick not persisted          |
| docs    | 2026-09-07 | done    | 8        | product.md tab list and save action     |
| retro   | 2026-09-07 | done    | 0        | -                                       |
| hygiene | 2026-09-07 | done    | 4        | two closed photo branches, Rajat's call |

## Deferred

### signals

- No query function lists `dishDislikes` (only the `dislikeDish` mutation and the internal mark-applied
  mutation touch it); this sitting read the table through the Convex CLI (empty). A chore-sized
  `queries/dishDislikes:listQueuedDislikes` is owed so the next sitting reads it like the other two tables.
- One protein added by hand beside a solo complete-meal lunch (Fish tikka beside Curd rice, 2026-09-07
  week, one member): one instance, one short of any threshold; recurrence across weeks or by both members
  becomes an evolution request stated as a household count.

### health

- The exploration pick is not persisted (`generatedPlan` carries day, meal, dishId only), so the
  swap-away measure reconstructs it by regenerating the week, which needs the favorites list as of
  generation time; a schema field plus a §16.1 contract line, owned by engineering (EM).
- The Saturday swap measure covers 4 of 8 served weeks because the sanctioned export lists queued rows
  only; a chore-sized `manualChanges` by-`weekStart` query (any status) is owed beside the dislikes one.
- The two v6 swap measures are provisional (the 2026-09-07 week was one day old at export); the second
  monitor re-reads both with the week complete.
- International is the row to watch (+37.8 percent on 19 rows against 32.4 percent noise, 0 of 11
  occasions in the v6 plan); not a finding on a first monitor.
- Stale preamble sentence in the gate report ("every week of this fixture predates cutover", wording in
  `engine/scripts/gate.ts`) now that an export carries a `generatedPlan`; for the docs pass.
- A validator for an Active dish with 0 ingredient rows (the reports cannot see one); a docs-and-engine
  chore, not this pass's.

### docs

- `docs/product.md` line 1 names the fourth tab as Changes; it is Yours (§6 and the app agree). Predates the window.
- `docs/product.md` §1, §4 Principle 4, and §9 still list "save a dish for next week" among the fast-loop
  actions; that action was removed with the Yours tab. Predates the window.
- `docs/CHANGELOG.md` format header names `/reconcile-docs` and `/reconcile-ops` as the `Updated:` line's
  consumers; needs one decision, whether the docs brief's header carve-out (RETRO.md only) widens to match
  `MAINTENANCE.md` §7 (any ledger's header) or §7 narrows.
- `docs/engineering.md` §2's Convex column omits `favorites`, `wishlist`, and `dishDislikes`. Predates the window.
- `docs/PLAN.md` Phase 2's outcome cell names the retired command shapes; a shipped phase's wording, left as a note.
- The gate report preamble sentence ("every week of this fixture predates cutover") comes from
  `engine/scripts/gate.ts` and is wrong for an export carrying a `generatedPlan`; a chore-sized conditional.
- `app/web/e2e/smoke.mjs` carries a phase-narrating code comment; left for the next e2e-touching stream.
- `README.md` says Explore hides "anything already planned or already on a shared list"; `docs/product.md` §3
  says only this week's plan is hidden; not verified against `getExploreFeed`, one of the two is wrong.

### retro

- (none)

### hygiene

- `data/photos-4` (#65) and `data/photos-7` (#73) survive closed, unmerged PRs with one commit each;
  closed-but-unmerged is Rajat's call, not the merged case this pass deletes.
- `comments` and `nextWeekQueue` remain as empty tables on the dev deployment `lovely-curlew-631`,
  retired from `app/convex/schema.ts`; dropping a table is Rajat's.
- `archive/features/UI Improvements/` breaks the kebab-case folder rule; a rename touches four name
  references, and this pass never renames.
- `/evolve-engine` still lives in the legacy `.claude/commands/` layout; the move to
  `.claude/skills/evolve-engine/` is a mechanical `git mv` plus a caller grep, deferred because
  nothing this sitting touched needs it.
